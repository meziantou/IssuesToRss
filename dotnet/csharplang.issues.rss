<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>dotnet/csharplang Issues</title>
    <description>Issues from https://github.com/dotnet/csharplang, generated by https://github.com/meziantou/IssuesToRss/</description>
    <managingEditor>meziantousite@outlook.com</managingEditor>
    <ttl>60</ttl>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4940</guid>
      <link>https://github.com/dotnet/csharplang/issues/4940</link>
      <title>Issue: Improve resolution of 'new expressions' for params - @kofifus</title>
      <description>&lt;h3&gt;Discussed in https://github.com/dotnet/csharplang/discussions/4939&lt;/h3&gt;
&lt;div type='discussions-op-text'&gt;
&lt;p&gt;&lt;sup&gt;Originally posted by &lt;strong&gt;kofifus&lt;/strong&gt; July 18, 2021&lt;/sup&gt;
Consider:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class MyList&amp;lt;T&amp;gt; {
  public MyList(params T[] ts) { /* add all items to internal list */ }
}

record Employee(string Name, int Age);

var l1 = new MyList&amp;lt;Employee&amp;gt;(new Employee(&amp;quot;John&amp;quot;, 30));   // ok

var l2 = new MyList&amp;lt;Employee&amp;gt;(new(&amp;quot;John&amp;quot;, 30));  // error CS8752: The type 'Program.Employee[]' may not be used as the target type of new()

var l3 = new MyList&amp;lt;Employee&amp;gt;(new(&amp;quot;John&amp;quot;, 30), new(&amp;quot;Eric&amp;quot;, 30)) // ok;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If my understanding is correct, the reason for the error in l2 is that according to the standard:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;A target_typed_new expression does not have a type. However, there is a new object creation conversion that is an implicit conversion from expression, that exists from a target_typed_new to every type.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;However in this case I believe the compiler can be improved to detect that the new expression needs to be a new &lt;code&gt;Employee&lt;/code&gt; and not &lt;code&gt;Employee[]&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Thanks&lt;/p&gt;
&lt;/div&gt;
</description>
      <pubDate>Sun, 18 Jul 2021 10:27:42 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4935</guid>
      <link>https://github.com/dotnet/csharplang/issues/4935</link>
      <title>Issue: [Proposal]: Remove restriction that interpolations within a non-verbatim interpolated string  cannot contain new-lines. - @CyrusNajmabadi</title>
      <description>&lt;h1&gt;Allow new-lines in all interpolations&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;The language today non-verbatim and verbatim interpolated strings (&lt;code&gt;$&amp;quot;&amp;quot;&lt;/code&gt; and &lt;code&gt;$@&amp;quot;&amp;quot;&lt;/code&gt; respectively).  The primary &lt;em&gt;sensible&lt;/em&gt; difference for these is that a non-verbatim interpolated string works like a normal string and cannot contain newlines in its text segments, and must instead use escapes (like &lt;code&gt;\r\n&lt;/code&gt;).  Conversely, a verbatim interpolated string can contain newlines in its text segments (like a verbatim string), and doesn't escape newlines or other character (except for &lt;code&gt;&amp;quot;&amp;quot;&lt;/code&gt; to escape a quote itself).&lt;/p&gt;
&lt;p&gt;This is all reasonable and will not change with this proposal.&lt;/p&gt;
&lt;p&gt;What is unreasonable today is that we extend the restriction on 'no newlines' in a non-verbatim interpolated string &lt;em&gt;beyond&lt;/em&gt; its text segments into the &lt;em&gt;interpolations&lt;/em&gt; themselves.  This means, for example, that you cannot write the following:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;var v = $&amp;quot;Count is\t: { this.Is.A.Really()
                            .That.I.Should(
                                be + able)[
                                    to.Wrap()] }.&amp;quot;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ultimately, the 'interpolation must be on a single line itself' rule is just a restriction of the current implementation.  That restriction really isn't necessary, and can be annoying, and would be fairly trivial to remove (see work https://github.com/dotnet/roslyn/pull/54875 to show how).   In the end, all it does is force the dev to place things on a single line, or force them into a verbatim interpolated string (both of which may be unpalatable).&lt;/p&gt;
&lt;p&gt;The interpolation expressions themselves are not text, and shouldn't be beholden to any escaping/newline rules therin.&lt;/p&gt;
</description>
      <pubDate>Fri, 16 Jul 2021 21:29:02 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4934</guid>
      <link>https://github.com/dotnet/csharplang/issues/4934</link>
      <title>Issue: [Proposal]: Explicit lambda return type - @jcouv</title>
      <description>&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Allow lambdas with explicit return type: &lt;code&gt;int (int x, int y) =&amp;gt; x + y&lt;/code&gt;.
Allow lambdas with attributes
Infer a natural delegate type for lambdas and method groups&lt;/p&gt;
&lt;p&gt;https://github.com/dotnet/csharplang/blob/main/proposals/lambda-improvements.md&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://github.com/dotnet/csharplang/blob/main/meetings/2021/LDM-2021-06-21.md (inference, &lt;code&gt;ref&lt;/code&gt; return syntax)&lt;/li&gt;
&lt;/ul&gt;
</description>
      <pubDate>Fri, 16 Jul 2021 18:54:11 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4907</guid>
      <link>https://github.com/dotnet/csharplang/issues/4907</link>
      <title>Issue: Global or Default Namespace Statements - @Turpy</title>
      <description>&lt;p&gt;Similar to the new global using statements, it would be nice if there was an option for a global namespace that could be set for a project so that if a namespace was not declared in a file then it would default to this global namespace.&lt;/p&gt;
&lt;p&gt;If a namespace was provided then it would override the globally set namespace.&lt;/p&gt;
&lt;p&gt;Also, it would be nice if there was a directory structure for source files underneath the top level directory for the project then the name of the directory can be appended automatically to the project level global namespace - again if none are provided in that file.&lt;/p&gt;
</description>
      <pubDate>Thu, 08 Jul 2021 18:15:18 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4905</guid>
      <link>https://github.com/dotnet/csharplang/issues/4905</link>
      <title>Issue: Pattern matching for event method - @Jogge</title>
      <description>&lt;p&gt;Often when I am using events I have to check wether the sender is a specific type like this:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;DataContextChanged += (sender, _) =&amp;gt;
{
    if (sender is MyViewModel myViewModel)
    {
        myViewModel.DoSomeThing();
    }
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Why is it not possible to do pattern matching in the signature of the method?&lt;/p&gt;
&lt;p&gt;Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;DataContextChanged += (sender is MyViewModel myViewModel, _) =&amp;gt;
{
    myViewModel.DoSomeThing();
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I would expect only to run &lt;code&gt;myViewModel.DoSomeThing();&lt;/code&gt; when &lt;code&gt;sender&lt;/code&gt; acutually is of type &lt;code&gt;MyViewModel&lt;/code&gt;. If it is not, I would expect the event to ignore this method.&lt;/p&gt;
</description>
      <pubDate>Mon, 05 Jul 2021 13:48:52 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4899</guid>
      <link>https://github.com/dotnet/csharplang/issues/4899</link>
      <title>Issue: [Proposal]: Multiple top-level statement compilation units - @Youssef1313</title>
      <description>&lt;!--
Hello, and thanks for your interest in contributing to C#! If you haven't been invited by a team member to open an issue, please instead open a discussion marked [draft issue] at https://github.com/dotnet/csharplang/discussions/new and we'll try to give you feedback on how to get to an issue-ready proposal.

New language feature proposals should fully fill out this template. This should include a complete detailed design, which describes the syntax of the feature, what that syntax means, and how it affects current parts of the spec. Please make sure to point out specific spec sections that need to be updated for this feature.
--&gt;
&lt;h1&gt;Multiple top-level statement compilation units&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Today, the language allows only one top-level compilation units because only one entry-point should be generated. This proposal allows multiple top-level compilation units with a restriction described below.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Writing top-level statement programs is a very neat feature. However, things get a bit messy when the file becomes large, usually due to multiple local functions being declared.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;This simply lift the restriction and allows multiple top-level files if and only if exactly one of these files contain non-&lt;code&gt;LocalFunctionStatement&lt;/code&gt; global statement. So, for example, allow the following:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;// File1.cs
Console.WriteLine(&amp;quot;Hello, world&amp;quot;);

// File2.cs
void M() { }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;but, disallow the following:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// File1.cs
Console.WriteLine(&amp;quot;Hello, world&amp;quot;);

// File2.cs
Console.WriteLine(&amp;quot;Hello, world 2&amp;quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Sat, 03 Jul 2021 15:01:11 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4874</guid>
      <link>https://github.com/dotnet/csharplang/issues/4874</link>
      <title>Issue: [Proposal]: allow using `namespaces` without curly braces - @ikourfaln</title>
      <description>&lt;h1&gt;allow using &lt;code&gt;namespaces&lt;/code&gt; without curly braces&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;We are heavily using Namespaces to organize and control the scope of classes and method names in larger programming projects.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Minimize the code you have to write&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;Examples:&lt;/p&gt;
&lt;p&gt;Currently:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;namespace SampleNamespace
{
    class SampleClass
    {
        public void SampleMethod()
        {
            System.Console.WriteLine(&amp;quot;SampleMethod inside SampleNamespace&amp;quot;);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Proposed:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;namespace SampleNamespace

class SampleClass
{
    public void SampleMethod()
    {
        System.Console.WriteLine(&amp;quot;SampleMethod inside SampleNamespace&amp;quot;);
    }
}
&lt;/code&gt;&lt;/pre&gt;
</description>
      <pubDate>Thu, 24 Jun 2021 19:21:51 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4856</guid>
      <link>https://github.com/dotnet/csharplang/issues/4856</link>
      <title>Issue: Issue compiling C# project: Unrecognized option: /deterministic - @rich0726</title>
      <description>&lt;p&gt;Hello!
Recently I've started having trouble compiling a C# based web application in VS2019.  The same issue is happening in multiple versions of VS2019, as far back as 16.4, and as far forward as 16.11 Preview 2.  I'm thinking my project file got corrupted somehow, because this is happening on multiple machines.&lt;/p&gt;
&lt;p&gt;The error is CS2007, reported by CSC, and reads &amp;quot;Unrecognized option: /deterministic+&amp;quot;&lt;/p&gt;
&lt;p&gt;This occurs whether or not I explicitly declare deterministic in the PropertyGroup in my .csproj file, and whether or not I set it to true or false.&lt;/p&gt;
&lt;p&gt;Note that prior to this, I was getting an error saying that Debugging information setting couldn't be set to &amp;quot;portable&amp;quot;, and that I'd have to use full or pdbonly.  I change to Full, and now I get this error.&lt;/p&gt;
&lt;p&gt;Any help is greatly appreciated!&lt;/p&gt;
</description>
      <pubDate>Mon, 21 Jun 2021 18:42:54 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4826</guid>
      <link>https://github.com/dotnet/csharplang/issues/4826</link>
      <title>Issue: [ExcludeFromCodeCoverage] attribute not available on method generated from excluded method - @jakubch1</title>
      <description>&lt;p&gt;Scenario is:
Method&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;        [ExcludeFromCodeCoverage]
        public async Task TestExcludedAsyncFunction()
        {
            await Task.Delay(1).ConfigureAwait(false);
        }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;will generate class: &lt;code&gt;.class nested private auto ansi sealed beforefieldinit '&amp;lt;TestExcludedAsyncFunction&amp;gt;d__2'&lt;/code&gt; which doesn't have any ExcludeFromCodeCoverage attribute so coverage tools will instrument and show coverage for it. We were trying to find ordinal number &lt;code&gt;2&lt;/code&gt; on initial method but we fail using Microsoft.Reflection.Metadata, so we can't match those 2 methods.&lt;/p&gt;
&lt;p&gt;Very similar issue we see for local methods, methods with yield, lambda. Could you please preserve &lt;code&gt;ExcludeFromCodeCoverage&lt;/code&gt; attribute in such cases?&lt;/p&gt;
&lt;p&gt;Related issue:
https://github.com/microsoft/vstest/issues/2658&lt;/p&gt;
</description>
      <pubDate>Thu, 10 Jun 2021 18:29:06 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4748</guid>
      <link>https://github.com/dotnet/csharplang/issues/4748</link>
      <title>Issue: [Proposal]: Directly invoked anonymous functions - @MadsTorgersen</title>
      <description>&lt;h1&gt;Directly invoked anonymous functions&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Allow (parenthesized) lambda expressions and anonymous methods to be directly invoked with an argument list:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;var s = o switch
{
    Person p =&amp;gt; (name =&amp;gt; { WriteLine(name); return name.Trim(); })(p.GetName()),
    ...
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Lambda expressions (together with anonymous methods) are also called &amp;quot;anonymous functions&amp;quot;. We are doing more and more to make them more similar to named local function declarations: Allowing the &lt;code&gt;static&lt;/code&gt; modifier, attributes and explicit return types. One thing we do not yet allow is invoking them directly. For that they still need to be converted to a delegate type.&lt;/p&gt;
&lt;p&gt;There are two main motivations to allow this:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;As a way to enable statements and scopes in an expression context&lt;/li&gt;
&lt;li&gt;To enhance the code environment; notably to make it async&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;From a grammar perspective direct invocation is already allowed, as long as the lambda expression is parenthesized:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-antlr"&gt;invocation_expression
    : primary_expression '(' argument_list? ')'
    ;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;There are currently two kinds of invocation expressions: &lt;a href="https://github.com/dotnet/csharpstandard/blob/draft-v6/standard/expressions.md#12762-method-invocations"&gt;Method invocations&lt;/a&gt; (including &lt;a href="https://github.com/dotnet/csharpstandard/blob/draft-v6/standard/expressions.md#12763-extension-method-invocations"&gt;extension method invocations&lt;/a&gt;) and &lt;a href="https://github.com/dotnet/csharpstandard/blob/draft-v6/standard/expressions.md#12764-delegate-invocations"&gt;delegate invocations&lt;/a&gt;. This proposal adds &amp;quot;anonymous function invocations&amp;quot; as follows:&lt;/p&gt;
&lt;p&gt;For an anonymous function invocation the &lt;code&gt;primary_expression&lt;/code&gt; shall be classified as an anonymous function. If the anonymous function is a &lt;code&gt;lambda_expression&lt;/code&gt; with an &lt;code&gt;implicit_anonymous_function_signature&lt;/code&gt; then the &lt;code&gt;argument_list&lt;/code&gt; shall have a corresponding number of arguments and all of these arguments shall have types. These types are then taken to be the types of the corresponding parameters.&lt;/p&gt;
&lt;p&gt;Considering the anonymous function to be a function member, it shall be &lt;a href="https://github.com/dotnet/csharpstandard/blob/draft-v6/standard/expressions.md#12642-applicable-function-member"&gt;applicable&lt;/a&gt; to the argument list.&lt;/p&gt;
&lt;p&gt;At runtime an anonymous function invocation is processed as a &lt;a href="https://github.com/dotnet/csharpstandard/blob/draft-v6/standard/expressions.md#1266-function-member-invocation"&gt;function member invocation&lt;/a&gt;.&lt;/p&gt;
&lt;h3&gt;A note on parentheses&lt;/h3&gt;
&lt;p&gt;As mentioned, anonymous function invocations can only parse if the anonymous function expression is parenthesized. While the spec doesn't seem to be entirely clear about this, parenthesized expressions can already be invoked today, and the kind of invocation hinges on the classification of the expression &lt;em&gt;inside&lt;/em&gt; the parentheses. Essentially, except for parsing, parentheses are ignored.&lt;/p&gt;
&lt;p&gt;This holds even when the parenthesized expression doesn't have a value; e.g. if it is a method group:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;void M(int i) =&amp;gt; WriteLine(i);

M(1);     // Normal method invocation
(M)(2);   // Disallowed only because of parsing ambiguity with cast expression
((M))(3); // Allowed; parentheses are ignored
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;We should probably consider making the spec clearer about this, but invocation of parenthesized expressions is nothing new, and is not part of this proposal.&lt;/p&gt;
&lt;h3&gt;A note on implementation&lt;/h3&gt;
&lt;p&gt;A directly invoked anonymous function is very much like a &lt;a href="https://github.com/dotnet/csharplang/blob/main/proposals/csharp-7.0/local-functions.md"&gt;local function&lt;/a&gt;. We should consider some of the same implementation strategies, e.g. passing as an extra ref argument a struct representing the closure.&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;Direct invocation of lambda expressions, even though common in other languages, does not have great readability.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;h3&gt;Natural delegate types&lt;/h3&gt;
&lt;p&gt;One alternative is for this to fall out of the &lt;a href="https://github.com/dotnet/csharplang/blob/main/proposals/lambda-improvements.md#natural-delegate-type"&gt;&amp;quot;natural types for lambda expressions&amp;quot;&lt;/a&gt; feature. Essentially when a lambda expression has a natural delegate type, we would create the delegate and immediately invoke it.&lt;/p&gt;
&lt;p&gt;The downsides are that it would not apply to lambdas without explicit parameter types, and that the semantics require a delegate to be created only to be immediately discarded (though this can likely be optimized away).&lt;/p&gt;
&lt;h3&gt;Expression blocks&lt;/h3&gt;
&lt;p&gt;The first scenario - fitting statements into an expression context - could be addressed by a dedicated feature, such as &lt;a href="https://github.com/dotnet/csharplang/issues/3086"&gt;expression blocks&lt;/a&gt;. This would require new syntax, and has been the subject of some controversy. It would not address the second scenario; the need to create an async context for code.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;No known unknowns.&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;p&gt;Direct invocation of lambda expressions was discussed in &lt;a href="https://github.com/dotnet/csharplang/blob/main/meetings/2021/LDM-2021-05-10.md#direct-invocation"&gt;C# LDM on May 10, 2021&lt;/a&gt;.&lt;/p&gt;
</description>
      <pubDate>Thu, 13 May 2021 17:49:49 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4747</guid>
      <link>https://github.com/dotnet/csharplang/issues/4747</link>
      <title>Issue: [Proposal]: Enhanced #line directives - @tmat</title>
      <description>&lt;h1&gt;Enhanced #line directives&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;The compiler applies the mapping defined by &lt;code&gt;#line&lt;/code&gt; directives to diagnostic locations and sequence points emitted to the PDB.&lt;/p&gt;
&lt;p&gt;Currently only the line number and file path can be mapped while the starting character is inferred from the source code. The proposal is to allow specifying full span mapping.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;DSLs that generate C# source code (such as ASP.NET Razor) can't currently produce precise source mapping using &lt;code&gt;#line&lt;/code&gt; directives. This results in degraded debugging experience in some cases as the sequence points emitted to the PDB can't map to the precise location in the original source code.&lt;/p&gt;
&lt;p&gt;For example, the following Razor code&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@page &amp;quot;/&amp;quot;
Time: @DateTime.Now
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;generates code like so (simplified):&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line hidden
void Render()
{
   _builder.Add(&amp;quot;Time:&amp;quot;);
#line 2 &amp;quot;page.razor&amp;quot;
   _builder.Add(DateTime.Now);
#line hidden
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The above directive would map the sequence point emitted by the compiler for the &lt;code&gt;_builder.Add(DateTime.Now);&lt;/code&gt; statement to the line 2, but the column would be off (16 instead of 7).&lt;/p&gt;
&lt;p&gt;The Razor source generator actually incorrectly generates the following code:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line hidden
void Render()
{
   _builder.Add(&amp;quot;Time:&amp;quot;);
   _builder.Add(
#line 2 &amp;quot;page.razor&amp;quot;
      DateTime.Now
#line hidden
);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The intent was to preserve the starting character and it works for diagnostic location mapping. However, this does not work for sequence points since &lt;code&gt;#line&lt;/code&gt; directive only applies to the sequence points that follow it. There is no sequence point in the middle of the  &lt;code&gt;_builder.Add(DateTime.Now);&lt;/code&gt; statement (sequence points can only be emitted at IL instructions with empty evaluation stack). The &lt;code&gt;#line 2&lt;/code&gt; directive in above code thus has no effect on the generated PDB and the debugger won't place a breakpoint or stop on the &lt;code&gt;@DateTime.Now&lt;/code&gt; snippet in the Razor page.&lt;/p&gt;
&lt;p&gt;Issues addressed by this proposal:
https://github.com/dotnet/roslyn/issues/43432
https://github.com/dotnet/roslyn/issues/46526&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;We amend the syntax of &lt;code&gt;line_indicator&lt;/code&gt; used in &lt;code&gt;pp_line&lt;/code&gt; directive like so:&lt;/p&gt;
&lt;p&gt;Current:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;line_indicator
    : decimal_digit+ whitespace file_name
    | decimal_digit+
    | 'default'
    | 'hidden'
    ;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Proposed:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;line_indicator
    : '(' decimal_digit+ ',' decimal_digit+ ')' '-' '(' decimal_digit+ ',' decimal_digit+ ')' decimal_digit+ whitespace file_name
    | '(' decimal_digit+ ',' decimal_digit+ ')' '-' '(' decimal_digit+ ',' decimal_digit+ ')' file_name
    | decimal_digit+ whitespace file_name
    | decimal_digit+
    | 'default'
    | 'hidden'
    ;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That is, the &lt;code&gt;#line&lt;/code&gt; directive would accept either 5 decimal numbers (&lt;em&gt;start line&lt;/em&gt;, &lt;em&gt;start character&lt;/em&gt;, &lt;em&gt;end line&lt;/em&gt;, &lt;em&gt;end character&lt;/em&gt;, &lt;em&gt;character offset&lt;/em&gt;),
4 decimal numbers (&lt;em&gt;start line&lt;/em&gt;, &lt;em&gt;start character&lt;/em&gt;, &lt;em&gt;end line&lt;/em&gt;, &lt;em&gt;end character&lt;/em&gt;), or a single one (&lt;em&gt;line&lt;/em&gt;).&lt;/p&gt;
&lt;p&gt;If &lt;em&gt;character offset&lt;/em&gt; is not specified its default value is 0, otherwise it specifies the number of UTF-16 characters. The number must be non-negative and less then length of the line following the #line directive in the unmapped file.&lt;/p&gt;
&lt;p&gt;(&lt;em&gt;start line&lt;/em&gt;, &lt;em&gt;start character&lt;/em&gt;)-(&lt;em&gt;end line&lt;/em&gt;, &lt;em&gt;end character&lt;/em&gt;) specifies a span in the mapped file. &lt;em&gt;start line&lt;/em&gt; and &lt;em&gt;end line&lt;/em&gt; are positive integers that specify line numbers. &lt;em&gt;start character&lt;/em&gt;, &lt;em&gt;end character&lt;/em&gt; are positive integers that specify UTF-16 character numbers. &lt;em&gt;start line&lt;/em&gt;, &lt;em&gt;start character&lt;/em&gt;, &lt;em&gt;end line&lt;/em&gt;, &lt;em&gt;end character&lt;/em&gt; are 1-based, meaning that the first line of the file and the first UTF-16 character on each line is assigned number 1.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The implementation would constraint these numbers so that they specify a valid &lt;a href="https://github.com/dotnet/runtime/blob/main/docs/design/specs/PortablePdb-Metadata.md#sequence-points-blob"&gt;sequence point source span&lt;/a&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;em&gt;start line&lt;/em&gt; - 1 is within range [0, 0x20000000) and not equal to 0xfeefee.&lt;/li&gt;
&lt;li&gt;&lt;em&gt;end line&lt;/em&gt; - 1 is within range [0, 0x20000000) and not equal to 0xfeefee.&lt;/li&gt;
&lt;li&gt;&lt;em&gt;start character&lt;/em&gt; - 1 is within range [0, 0x10000)&lt;/li&gt;
&lt;li&gt;&lt;em&gt;end character&lt;/em&gt; - 1 is within range [0, 0x10000)&lt;/li&gt;
&lt;li&gt;&lt;em&gt;end line&lt;/em&gt; is greater or equal to &lt;em&gt;start line&lt;/em&gt;.&lt;/li&gt;
&lt;li&gt;&lt;em&gt;start line&lt;/em&gt; is equal to &lt;em&gt;end line&lt;/em&gt; then &lt;em&gt;end character&lt;/em&gt; is greater than &lt;em&gt;start character&lt;/em&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;Note that the numbers specified in the directive syntax are 1-based numbers but the actual spans in the PDB are zero-based. Hence the -1 adjustments above.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The mapped spans of sequence points and the locations of diagnostics that &lt;code&gt;#line&lt;/code&gt; directive applies to are calculated as follows.&lt;/p&gt;
&lt;p&gt;Let &lt;em&gt;d&lt;/em&gt; be the zero-based number of the unmapped line containing the &lt;code&gt;#line&lt;/code&gt; directive.
Let span L = (start: (&lt;em&gt;start line&lt;/em&gt; - 1, &lt;em&gt;start character&lt;/em&gt; - 1), end: (&lt;em&gt;end line&lt;/em&gt; - 1, &lt;em&gt;end character&lt;/em&gt; - 1)) be zero-based span specified by the directive.&lt;/p&gt;
&lt;p&gt;Function M that maps a position (line, character) within the scope of the &lt;code&gt;#line&lt;/code&gt; directive in the source file containing the #line directive to a mapped position (mapped line, mapped character) is defined as follows:&lt;/p&gt;
&lt;p&gt;&lt;em&gt;M&lt;/em&gt;(&lt;em&gt;l&lt;/em&gt;, &lt;em&gt;c&lt;/em&gt;) =&lt;/p&gt;
&lt;p&gt;&lt;em&gt;l&lt;/em&gt; = &lt;em&gt;d&lt;/em&gt; + 1		=&amp;gt; 	(&lt;em&gt;L.start.line&lt;/em&gt; + &lt;em&gt;l&lt;/em&gt; – &lt;em&gt;d&lt;/em&gt; – 1, &lt;em&gt;L.start.character&lt;/em&gt; + max(&lt;em&gt;c&lt;/em&gt; – &lt;em&gt;character offset&lt;/em&gt;, 0))
&lt;em&gt;l&lt;/em&gt; &amp;gt; &lt;em&gt;d&lt;/em&gt; + 1		=&amp;gt; 	(&lt;em&gt;L.start.line&lt;/em&gt; + &lt;em&gt;l&lt;/em&gt; – &lt;em&gt;d&lt;/em&gt; – 1, &lt;em&gt;c&lt;/em&gt;)&lt;/p&gt;
&lt;p&gt;The syntax constructs that sequence points are associated with are determined by the compiler implementation and not covered by this specification.
The compiler also decides for each sequence point its unmapped span. This span may partially or fully cover the associated syntax construct.&lt;/p&gt;
&lt;p&gt;Once the unmapped spans are determined by the compiler the function &lt;em&gt;M&lt;/em&gt; defined above is applied to their starting and ending positions, with the exception of the ending position of all sequence points within the scope of the #line directive whose unmapped location is at line &lt;em&gt;d&lt;/em&gt; + 1 and character less than character offset. The end position of all these sequence points is &lt;em&gt;L.end&lt;/em&gt;.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Example [5.i] demonstrates why it is necessary to provide the ability to specify the end position of the first sequence point span.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;The above definition allows the generator of the unmapped source code to avoid intimate knowledge of which exact source constructs of the C# language produce sequence points. The mapped spans of the sequence points in the scope of the &lt;code&gt;#line&lt;/code&gt; directive are derived from the relative position of the corresponding unmapped spans to the first unmapped span.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;Specifying the &lt;em&gt;character offset&lt;/em&gt; allows the generator to insert any single-line prefix on the first line. This prefix is generated code that is not present in the mapped file. Such inserted prefix affects the value of the first unmapped sequence point span. Therefore the starting character of subsequent sequence point spans need to be offset by the length of the prefix (&lt;em&gt;character offset&lt;/em&gt;). See example [2].&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src="https://user-images.githubusercontent.com/41759/120511514-563c7e00-c37f-11eb-9436-20c2def68932.png" alt="image" /&gt;&lt;/p&gt;
&lt;h3&gt;Examples&lt;/h3&gt;
&lt;p&gt;For clarity the examples use &lt;code&gt;spanof('...')&lt;/code&gt; and &lt;code&gt;lineof('...')&lt;/code&gt; pseudo-syntax to express the mapped span start position and line number, respectively, of the specified code snippet.&lt;/p&gt;
&lt;h4&gt;1. First and subsequent spans&lt;/h4&gt;
&lt;p&gt;Consider the following code with unmapped zero-based line numbers listed on the right:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#line 1 10 1 15 &amp;quot;a&amp;quot;   // 3
  A();B(              // 4
);C();                // 5
    D();              // 6
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;d = 3
L = (0, 9)..(0, 14)&lt;/p&gt;
&lt;p&gt;There are 4 sequence point spans the directive applies to with following unmapped and mapped spans:
(4, 2)..(4, 5) =&amp;gt; (0, 9)..(0, 14)
(4, 6)..(5, 1) =&amp;gt; (0, 15)..(1, 1)
(5, 2)..(5, 5) =&amp;gt; (1, 2)..(1, 5)
(6, 4)..(6, 7) =&amp;gt; (2, 4)..(2, 7)&lt;/p&gt;
&lt;h4&gt;2. Character offset&lt;/h4&gt;
&lt;p&gt;Razor generates &lt;code&gt;  _builder.Add(&lt;/code&gt; prefix of length 15 (including two leading spaces).&lt;/p&gt;
&lt;p&gt;Razor:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@page &amp;quot;/&amp;quot;                                  
@F(() =&amp;gt; 1+1, 
   () =&amp;gt; 2+2
) 
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Generated C#:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line hidden
void Render()            
{ 
#line spanof('F(...)') 15 &amp;quot;page.razor&amp;quot;  // 4
  _builder.Add(F(() =&amp;gt; 1+1,            // 5
  () =&amp;gt; 2+2                            // 6
));                                    // 7
#line hidden
}
);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;d = 4
L = (1, 1)..(3,0)
&lt;em&gt;character offset&lt;/em&gt; = 15&lt;/p&gt;
&lt;p&gt;Spans:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;_builder.Add(F(…));&lt;/code&gt; =&amp;gt; &lt;code&gt;F(…)&lt;/code&gt;: (5, 2)..(7, 2) =&amp;gt; (1, 1)..(3, 0)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;1+1&lt;/code&gt; =&amp;gt; &lt;code&gt;1+1&lt;/code&gt;: (5, 23)..(5, 25) =&amp;gt; (1, 9)..(1, 11)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;2+2&lt;/code&gt; =&amp;gt; &lt;code&gt;2+2&lt;/code&gt;: (6, 7)..(6, 9) =&amp;gt; (2, 7)..(2, 9)&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;3. Razor: Single-line span&lt;/h4&gt;
&lt;p&gt;Razor:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@page &amp;quot;/&amp;quot;
Time: @DateTime.Now
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Generated C#:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line hidden
void Render()
{
  _builder.Add(&amp;quot;Time:&amp;quot;);
#line spanof('DateTime.Now') 15 &amp;quot;page.razor&amp;quot;
  _builder.Add(DateTime.Now);
#line hidden
);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;4. Razor: Multi-line span&lt;/h4&gt;
&lt;p&gt;Razor:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@page &amp;quot;/&amp;quot;                                  
@JsonToHtml(@&amp;quot;
{
  &amp;quot;&amp;quot;key1&amp;quot;&amp;quot;: &amp;quot;value1&amp;quot;,
  &amp;quot;&amp;quot;key2&amp;quot;&amp;quot;: &amp;quot;value2&amp;quot;
}&amp;quot;) 
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Generated C#:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line hidden
void Render()
{
  _builder.Add(&amp;quot;Time:&amp;quot;);
#line spanof('JsonToHtml(@&amp;quot;...&amp;quot;)') 15 &amp;quot;page.razor&amp;quot;
  _builder.Add(JsonToHtml(@&amp;quot;
{
  &amp;quot;&amp;quot;key1&amp;quot;&amp;quot;: &amp;quot;value1&amp;quot;,
  &amp;quot;&amp;quot;key2&amp;quot;&amp;quot;: &amp;quot;value2&amp;quot;
}&amp;quot;));
#line hidden
}
);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;5. Razor: block constructs&lt;/h4&gt;
&lt;h5&gt;i. block containing expressions&lt;/h5&gt;
&lt;p&gt;In this example, the mapped span of the first sequence point that is associated with the IL instruction that is emitted for the &lt;code&gt;_builder.Add(Html.Helper(() =&amp;gt; &lt;/code&gt; statement
needs to cover the whole expression of &lt;code&gt;Html.Helper(...)&lt;/code&gt; in the generated file &lt;code&gt;a.razor&lt;/code&gt;. This is achieved by application of rule [1] to the end position of the sequence point.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@Html.Helper(() =&amp;gt; 
{
    &amp;lt;p&amp;gt;Hello World&amp;lt;/p&amp;gt;
    @DateTime.Now
})
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line spanof('Html.Helper(() =&amp;gt; { ... })') 13 &amp;quot;a.razor&amp;quot;
_builder.Add(Html.Helper(() =&amp;gt; 
#line lineof('{') &amp;quot;a.razor&amp;quot;
{
#line spanof('DateTime.Now') 13 &amp;quot;a.razor&amp;quot;
_builder.Add(DateTime.Now);
#line lineof('}') &amp;quot;a.razor&amp;quot;
}
#line hidden
)
&lt;/code&gt;&lt;/pre&gt;
&lt;h5&gt;ii. block containing statements&lt;/h5&gt;
&lt;p&gt;Uses existing &lt;code&gt;#line line file&lt;/code&gt; form since&lt;/p&gt;
&lt;p&gt;a) Razor does not add any prefix,
b) &lt;code&gt;{&lt;/code&gt; is not present in the generated file and there can't be a sequence point placed on it, therefore the span of the first unmapped sequence point is unknown to Razor.&lt;/p&gt;
&lt;p&gt;The starting character of &lt;code&gt;Console&lt;/code&gt; in the generated file must be aligned with the Razor file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@{Console.WriteLine(1);Console.WriteLine(2);}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line lineof('@{') &amp;quot;a.razor&amp;quot;
  Console.WriteLine(1);Console.WriteLine(2);
#line hidden
&lt;/code&gt;&lt;/pre&gt;
&lt;h5&gt;iii. block containing top-level code (@code, @functions)&lt;/h5&gt;
&lt;p&gt;Uses existing &lt;code&gt;#line line file&lt;/code&gt; form since&lt;/p&gt;
&lt;p&gt;a) Razor does not add any prefix,
b) &lt;code&gt;{&lt;/code&gt; is not present in the generated file and there can't be a sequence point placed on it, therefore the span of the first unmapped sequence point is unknown to Razor.&lt;/p&gt;
&lt;p&gt;The starting character of &lt;code&gt;[Parameter]&lt;/code&gt; in the generated file must be aligned with the Razor file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@code {
    [Parameter]
    public int IncrementAmount { get; set; }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line lineof('[') &amp;quot;a.razor&amp;quot;
    [Parameter]
    public int IncrementAmount { get; set; }
#line hidden
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;6. Razor: &lt;code&gt;@for&lt;/code&gt;, &lt;code&gt;@foreach&lt;/code&gt;, &lt;code&gt;@while&lt;/code&gt;, &lt;code&gt;@do&lt;/code&gt;, &lt;code&gt;@if&lt;/code&gt;, &lt;code&gt;@switch&lt;/code&gt;, &lt;code&gt;@using&lt;/code&gt;, &lt;code&gt;@try&lt;/code&gt;, &lt;code&gt;@lock&lt;/code&gt;&lt;/h4&gt;
&lt;p&gt;Uses existing &lt;code&gt;#line line file&lt;/code&gt; form since
a) Razor does not add any prefix.
b) the span of the first unmapped sequence point may not be known to Razor (or shouldn't need to know).&lt;/p&gt;
&lt;p&gt;The starting character of the keyword in the generated file must be aligned with the Razor file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@for (var i = 0; i &amp;lt; 10; i++)
{
}
@if (condition)
{
}
else
{
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;#line lineof('for') &amp;quot;a.razor&amp;quot;
 for (var i = 0; i &amp;lt; 10; i++)
{
}
#line lineof('if') &amp;quot;a.razor&amp;quot;
 if (condition)
{
}
else
{
}
#line hidden
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Thu, 13 May 2021 00:52:14 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4745</guid>
      <link>https://github.com/dotnet/csharplang/issues/4745</link>
      <title>Issue: [Proposal]: ignored directives - @jcouv</title>
      <description>&lt;h1&gt;ignored directives&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;The feature is to allow some directives to be parsed and ignored by the language (like trivia), such as &lt;code&gt;#! ...&lt;/code&gt; and &lt;code&gt;#r ...&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;We'd like to allow scenarios like &lt;code&gt;dotnet run hello.cs&lt;/code&gt;. This involves embedding some information which traditionally belonged in a project file into a C# file.
See &lt;a href="https://github.com/dotnet/designs/pull/213"&gt;broader design proposal&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;LDM 5/12/2021&lt;/li&gt;
&lt;/ul&gt;
</description>
      <pubDate>Wed, 12 May 2021 19:23:31 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4729</guid>
      <link>https://github.com/dotnet/csharplang/issues/4729</link>
      <title>Issue: [Proposal]: Union types - @savans2</title>
      <description>&lt;h1&gt;Union types&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;In TypeScript we have union types where we can say that variable &lt;code&gt;status&lt;/code&gt; can be only one of the following values &lt;code&gt;&amp;quot;On&amp;quot; | &amp;quot;Off&amp;quot; | &amp;quot;Idle&amp;quot;&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;type status = &amp;quot;On&amp;quot; | &amp;quot;Off&amp;quot; | &amp;quot;Idle&amp;quot;

const whatsTheStatus: status = &amp;quot;On&amp;quot;; // can be only &amp;quot;On&amp;quot; or &amp;quot;Off&amp;quot; or &amp;quot;Idle&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;!-- Why are we doing this? What use cases does it support? What is the expected outcome? --&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;!-- This is the bulk of the proposal. Explain the design in enough detail for somebody familiar with the language to understand, and for somebody familiar with the compiler to implement, and include examples of how the feature is used. Please include syntax and desired semantics for the change, including linking to the relevant parts of the existing C# spec to describe the changes necessary to implement this feature. An initial proposal does not need to cover all cases, but it should have enough detail to enable a language team member to bring this proposal to design if they so choose. --&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Sat, 08 May 2021 13:50:12 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4724</guid>
      <link>https://github.com/dotnet/csharplang/issues/4724</link>
      <title>Issue: Proposal: Allow nullable types in declaration patterns - @alrz</title>
      <description>&lt;h1&gt;Allow nullable types in declaration patterns&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Allows the type to be specified in a var-pattern without change in semantics. This was originally proposed at https://github.com/dotnet/csharplang/issues/2039 but discarded due to possible behavioral change (see https://github.com/dotnet/csharplang/issues/2039#issuecomment-444234519 for an example).&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Currently, specifying the type in patterns has the side effect of checking for null which may not be the intent. Using nullable annotation would allow the type to be specified for readability without the added null check.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;This would be only allowed if the type has an identity conversion to the &lt;em&gt;input type&lt;/em&gt;, therefore, preserving the var-pattern semantics. The added restriction would make this mostly a code style preference and a safe replacement for var-pattern.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;bool M(object o) =&amp;gt; o is int? x;     // error
bool M(int? o) =&amp;gt; o is int? x;       // ok - same as `o is var x`
bool M(string? o) =&amp;gt; o is string? x; // ok - same as `o is var x`
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Note that the input type may not be immediately obvious, for example in a property pattern: &lt;code&gt;{ Prop: string? str }&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;If the original input is already in a not-null state, a nullable type syntax will drop the null check which is currently emitted regardless of type annotations on the input type. However, in this case, the variable would still have the not-null state, as it is the case for a var-pattern in the same situation.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Would it be safe to allow implicit reference conversions as well?&lt;/li&gt;
&lt;li&gt;What is the expected behavior in null-oblivious context for value and reference types? Could we allow one but not the other?&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;p&gt;None.&lt;/p&gt;
</description>
      <pubDate>Wed, 05 May 2021 19:36:39 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4723</guid>
      <link>https://github.com/dotnet/csharplang/issues/4723</link>
      <title>Issue: [Proposal] Extend "new" Generic Type Constraint  - @andyolivares</title>
      <description>&lt;h1&gt;Extend &amp;quot;new&amp;quot; Generic Type Constraint&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype&lt;/li&gt;
&lt;li&gt;[ ] Implementation&lt;/li&gt;
&lt;li&gt;[ ] Specification&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Extend the &lt;a href="https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/new-constraint"&gt;&lt;code&gt;new&lt;/code&gt; generic type constraint&lt;/a&gt; to be able to specify constructors other than public parameterless one. It could be useful to be able to type something like this:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;public class MyGeneric&amp;lt;T&amp;gt; where T : new(string, int)
{
    public void MyMethod()
    {
        ...
        var t = new T(&amp;quot;foobar&amp;quot;, 123);
        ...
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Also, I see no reason why we shouldn't allow multiple &amp;quot;new&amp;quot; constraints to be specified, for example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;public class MyGeneric&amp;lt;T&amp;gt; where T : new(), new(string, int)
{
    public void MyMethod()
    {
        ...
        var t1 = new T(); // Valid
        var t2 = new T(&amp;quot;foobar&amp;quot;, 123); // Also valid
        var t3 = new T(true); // Invalid
        ...
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In this last case, the compiler has to make sure that &lt;code&gt;T&lt;/code&gt; has at least 2 constructors, one parameterless and another one with parameters &lt;code&gt;string&lt;/code&gt; and &lt;code&gt;int&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;I see good use to something like this specially when creating immutable classes. As of today, there is no easy way to create immutable instances within a generic type (at least that I know of) without recurring to other means like factories, etc.&lt;/p&gt;
&lt;p&gt;Also, it doesn't seem like a breaking change that shouldn't be there at least for completeness of the language.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;N/A&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;I don't see a good reason :)&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;One can use other means/workarounds, like the use of other &amp;quot;factory&amp;quot; types that could receive arguments.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;N/A&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;p&gt;N/A&lt;/p&gt;
</description>
      <pubDate>Wed, 05 May 2021 05:02:53 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4706</guid>
      <link>https://github.com/dotnet/csharplang/issues/4706</link>
      <title>Issue: Assign properties in constructor parameters  - @navidzehi</title>
      <description>&lt;p&gt;I have the following proposal :
Assign properties in constructor parameters .&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class Person {

   //   1. constructor without body:
   
  public Person (this.FirstName, this.LastName);

   //   2. with body :
   
  public Person (this.FirstName, string lastName){
     LastName  = lastName;
   }
   public string FirstName {get;set;}
   public string LastName {get;set;}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The closest alternative is using Records.&lt;/p&gt;
</description>
      <pubDate>Wed, 28 Apr 2021 11:45:12 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4694</guid>
      <link>https://github.com/dotnet/csharplang/issues/4694</link>
      <title>Issue: [Proposal]: Static local - @SamiG42</title>
      <description>&lt;p&gt;Static local&lt;/p&gt;
</description>
      <pubDate>Sun, 25 Apr 2021 21:24:38 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4682</guid>
      <link>https://github.com/dotnet/csharplang/issues/4682</link>
      <title>Issue: [Proposal]: Unsigned right shift operator - @AlekseyTs</title>
      <description>&lt;h1&gt;Unsigned right shift operator&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;An unsigned right shift operator will be supported by C# as a built-in operator (for primitive integral types) and as a user-defined operator.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;When working with signed integral value, it is not uncommon that you need to shift bits right without replicating the high order bit on each shift. While this can be achieved for primitive integral types with a regular shift operator, a cast to an unsigned type before the shift operation and a cast back after it is required. Within the context of the generic math interfaces the libraries are planning to expose, this is potentially more problematic as the type might not necessary have an unsigned counterpart defined or known upfront by the generic math code, yet an algorithm might rely on ability to perform an unsigned right shift operation.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;There are some considerations around what the operator could be and the initial suggestion is &lt;code&gt;&amp;gt;&amp;gt;&amp;gt;&lt;/code&gt;, to match other languages. It is suggested that &lt;code&gt;&amp;gt;&amp;gt;&amp;gt;&lt;/code&gt; be explicitly an unsigned right shift and &lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt; would continue being just “right shift” with no strict definition. For primitive unsigned integral types &lt;code&gt;&amp;gt;&amp;gt;&amp;gt;&lt;/code&gt; will be allowed, but will be equivalent to &lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Thu, 22 Apr 2021 22:22:18 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4674</guid>
      <link>https://github.com/dotnet/csharplang/issues/4674</link>
      <title>Issue: Overload resolution and inferred lambda / method group delegate type: potential breaking change - @cston</title>
      <description>&lt;p&gt;The &lt;a href="https://github.com/dotnet/csharplang/blob/main/proposals/lambda-improvements.md#natural-delegate-type"&gt;lambda improvements proposal&lt;/a&gt; includes inferring delegate types for lambdas and method groups, creating the possibility of breaking changes in overload resolution.&lt;/p&gt;
&lt;h3&gt;Example 1&lt;/h3&gt;
&lt;p&gt;Two overloads in the default method group &lt;code&gt;M&lt;/code&gt;, one with a generic parameter type, the other with a delegate type parameter. In C#9, &lt;code&gt;M(F)&lt;/code&gt; binds to &lt;code&gt;M(Action&amp;lt;string&amp;gt; a)&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;If a delegate type is inferred for &lt;code&gt;F&lt;/code&gt; of &lt;code&gt;Action&amp;lt;object&amp;gt;&lt;/code&gt;, overload resolution will bind to the generic overload &lt;code&gt;M&amp;lt;Action&amp;lt;object&amp;gt;&amp;gt;(Action&amp;lt;object&amp;gt; t)&lt;/code&gt; instead.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;using System;
 
class Program
{
    static void M&amp;lt;T&amp;gt;(T t) { }
    static void M(Action&amp;lt;string&amp;gt; a) { }
    
    static void F(object o) { }
    
    static void Main()
    {
        M(F); // C#9: M(Action&amp;lt;string&amp;gt;)
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;See &lt;a href="https://sharplab.io/#v2:EYLgtghglgdgNAFxAJwK7wCYgNQB8ACATAIwCwAUAAQVGX7EDsFA3hZe3cQGx0AslAWQA8AFQB8AChGUEASkrNKAXzYd6PfPwET6hIfQAMYyhHmKVVDqvbq+lAGISA9sABWAUwDGCSk7PLraksbbjsBaBgJWUDWYI52bXtZAG5KAHo0ygBhAGIAThBBHWI9QzFouIslIA==="&gt;sharplab.io&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;Example 2&lt;/h3&gt;
&lt;p&gt;Two overloads in the method group &lt;code&gt;c.M&lt;/code&gt;, one with an &lt;code&gt;object&lt;/code&gt; type parameter on the containing type, the other with a delegate type parameter as an extension method. In C#9, the calls to &lt;code&gt;c.M(expr)&lt;/code&gt; bind to the extension method.&lt;/p&gt;
&lt;p&gt;If delegate types are inferred for the method group and lambda expression, those expressions are implicitly convertible to &lt;code&gt;object&lt;/code&gt; and overload resolution will bind the calls to the overload on the containing type instead.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;using System;

class Program
{
    static void Main()
    {
        var c = new C();
        c.M(Main);      // C#9: E.M(object x, Action y)
        c.M(() =&amp;gt; { }); // C#9: E.M(object x, Action y)
    }
}

class C
{
    public void M(object y) { }
}

static class E
{
    public static void M(this object x, Action y) { }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;See &lt;a href="https://sharplab.io/#v2:"&gt;sharplab.io&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;Possible solution&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;Prefer binding lambda or method group arguments using the target type (the parameter type) rather than the inferred delegate type of the lambda expression or method group.&lt;/li&gt;
&lt;li&gt;If extension methods should be considered, and binding of the method group from the containing type or any inner extension method scope used the inferred type for lambda or method group arguments, continue searching outer extension method scopes for an overload that binds using the parameter type.&lt;/li&gt;
&lt;/ol&gt;
</description>
      <pubDate>Tue, 20 Apr 2021 04:36:08 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4666</guid>
      <link>https://github.com/dotnet/csharplang/issues/4666</link>
      <title>Issue: [Proposal]: Relaxing shift operator requirements - @tannergooding</title>
      <description>&lt;h1&gt;Relaxing shift operator requirements&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;The shift operator requirements will be relaxed so that the right-hand side operator is no longer restricted to only be &lt;code&gt;int&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;When working with types other than &lt;code&gt;int&lt;/code&gt;, it is not uncommon that you shift using the result of another computation, such as shifting based on the &lt;code&gt;leading zero count&lt;/code&gt;. The natural type of something like a &lt;code&gt;leading zero count&lt;/code&gt; is the same as the input type (&lt;code&gt;TSelf&lt;/code&gt;) and so in many cases, this requires you to convert that result to &lt;code&gt;int&lt;/code&gt; before shifting, even if that result is already within range.&lt;/p&gt;
&lt;p&gt;Within the context of the generic math interfaces the libraries are planning to expose, this is potentially problematic as the type is not well known and so the conversion to &lt;code&gt;int&lt;/code&gt; may not be possible or even well-defined.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;https://github.com/dotnet/csharplang/blob/main/spec/expressions.md#shift-operators should be reworded as follows:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-diff"&gt;- When declaring an overloaded shift operator, the type of the first operand must always be the class or struct containing the operator declaration, and the type of the second operand must always be int.
+ When declaring an overloaded shift operator, the type of the first operand must always be the class or struct containing the operator declaration.
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That is, the restriction that the first operand be the class or struct containing the operator declaration remains. While the restriction that the second operand must be &lt;code&gt;int&lt;/code&gt; is removed.&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;Users will be able to define operators that do not follow the recommended guidelines, such as implementing &lt;code&gt;cout &amp;lt;&amp;lt; &amp;quot;string&amp;quot;&lt;/code&gt; in C#.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;The generic math interfaces being exposed by the libraries could expose explicitly named methods instead. This may make code more difficult to read/maintain.&lt;/p&gt;
&lt;p&gt;The generic math interfaces could require the shift take &lt;code&gt;int&lt;/code&gt; and that a conversion be performed. This conversion may be expensive or may be not possible depending on the type in question.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;Is there concern around preserving the &amp;quot;intent&amp;quot; around why the second operand was restricted to &lt;code&gt;int&lt;/code&gt;?&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Sat, 17 Apr 2021 00:02:43 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4665</guid>
      <link>https://github.com/dotnet/csharplang/issues/4665</link>
      <title>Issue: [Proposal]: Checked Operators - @tannergooding</title>
      <description>&lt;h1&gt;Checked Operators&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;C# should support defining &lt;code&gt;checked&lt;/code&gt; variants of all operators so that users can opt into or out of overflow behavior as appropriate.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;There is no way for a user to declare a type and support both checked and unchecked versions of an operator. This will make it hard to port various algorithms to use the proposed &lt;code&gt;generic math&lt;/code&gt; interfaces exposed by the libraries team. Likewise, this makes it impossible to expose a type such as &lt;code&gt;Int128&lt;/code&gt; or &lt;code&gt;UInt128&lt;/code&gt; without the language simultaneously shipping its own support to avoid breaking changes.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;C# will support the &lt;code&gt;checked&lt;/code&gt; keyword as a modifier on operators. For example: &lt;code&gt;static T checked operator +(T lhs, T rhs);&lt;/code&gt;
For brevity below, an operator with the &lt;code&gt;checked&lt;/code&gt; keyword as a modifier is referred to as a &lt;code&gt;checked operator&lt;/code&gt; and an operator without such a modifier is referred to as a &lt;code&gt;regular operator&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;A &lt;code&gt;checked operator&lt;/code&gt; will be emitted with the same name as the regular operator but with an additional &lt;code&gt;Checked&lt;/code&gt; suffix. For example: &lt;code&gt;op_AdditionChecked&lt;/code&gt; in contrast to &lt;code&gt;op_Addition&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;The compiler will require that if a &lt;code&gt;checked operator&lt;/code&gt; is defined, a corresponding &lt;code&gt;regular operator&lt;/code&gt; is also defined. The compiler will not require a &lt;code&gt;checked operator&lt;/code&gt; to be defined if only a &lt;code&gt;regular operator&lt;/code&gt; exists. This preserves backwards compatibility.&lt;/p&gt;
&lt;p&gt;The compiler, when performing member lookup to find candidate user-defined operators within an &lt;code&gt;unchecked&lt;/code&gt; context, will continue to perform lookup as it does today. If metadata is encountered that only defines a &lt;code&gt;checked operator&lt;/code&gt;, then it will not be considered as an applicable member function and a compilation error will occur.&lt;/p&gt;
&lt;p&gt;The compiler, when performing member lookup to find candidate user-defined operators within a &lt;code&gt;checked&lt;/code&gt; context will also consider applicable operators ending with &lt;code&gt;Checked&lt;/code&gt;. That is, if the compiler was attempting to find applicable function members for the binary addition operator, it would look for both &lt;code&gt;op_Addition&lt;/code&gt; and &lt;code&gt;op_AdditionChecked&lt;/code&gt;. If the only applicable function member is a &lt;code&gt;checked operator&lt;/code&gt;, it will be used. If both a &lt;code&gt;regular operator&lt;/code&gt; and &lt;code&gt;checked operator&lt;/code&gt; exist and are equally applicable the &lt;code&gt;checked operator&lt;/code&gt; will be preferred. If both a &lt;code&gt;regular operator&lt;/code&gt; and a &lt;code&gt;checked operator&lt;/code&gt; exist but the &lt;code&gt;regular operator&lt;/code&gt; is an exact match while the &lt;code&gt;checked operator&lt;/code&gt; is not, the compiler will prefer the &lt;code&gt;regular operator&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The compiler will treat the default context of a &lt;code&gt;checked operator&lt;/code&gt; as &lt;code&gt;checked&lt;/code&gt;. The developer will need to explicitly use &lt;code&gt;unchecked&lt;/code&gt; if part of their algorithm should not participate in the &lt;code&gt;checked context&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The compiler will continue treating the default context of a &lt;code&gt;regular operator&lt;/code&gt; as it does today. If the context for the compilation is &lt;code&gt;checked&lt;/code&gt;, then it will continue to be emitted as a &lt;code&gt;regular operator&lt;/code&gt;. A &lt;code&gt;checked operator&lt;/code&gt; is differentiated only based on the presence of the &lt;code&gt;checked&lt;/code&gt; keyword as a modifier directly on the method signature. The compiler could consider introducing a warning wave for this scenario to help reduce the chance for accidental bugs.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;public struct Int128
{
    // This is fine, both a checked and regular operator are defined
    public static Int128 checked operator +(Int128 lhs, Int128 rhs);
    public static Int128 operator +(Int128 lhs, Int128 rhs);

    // This is fine, only a regular operator is defined
    public static Int128 operator -(Int128 lhs, Int128 rhs);

    // This should error, a regular operator must also be defined
    public static Int128 checked operator *(Int128 lhs, Int128 rhs);
}

public class MyClass
{
    public static void Add(Int128 lhs, Int128 rhs)
    {
        // Resolves to `op_AdditionChecked`
        Int128 r1 = checked(lhs + rhs);

        // Resolves to `op_Addition`
        Int128 r2 = unchecked(lhs + rhs);

        // Resolve to `op_Subtraction`
        Int128 r3 = checked(lhs - rhs);

        // Resolve to `op_Subtraction`
        Int128 r4 = unchecked(lhs - rhs);

        /* The following assumes `operator *` was not defined in C# and instead comes from an external reference */

        // Resolves to `op_MultiplyChecked`
        Int128 r5 = checked(lhs * rhs);

        // Error: Operator '*' cannot be applied to operands of type 'Int128' and 'Int128'
        Int128 r5 = unchecked(lhs * rhs);
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;This adds additional complexity to the language and allows users to introduce more kinds of breaking changes to their types.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;The generic math interfaces that the libraries plans to expose could expose named methods (such as &lt;code&gt;AddChecked&lt;/code&gt;). The primary drawback is that this is less readable/maintainable and doesn't get the benefit of the language precedence rules around operators.&lt;/p&gt;
&lt;p&gt;The language could support &lt;code&gt;op_Addition&lt;/code&gt;, &lt;code&gt;op_AdditionChecked&lt;/code&gt;, and &lt;code&gt;op_AdditionUnchecked&lt;/code&gt; to help limit the number of breaking changes This adds another layer of complexity that is likely not necessary in most code.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;Should the language additionally allow &lt;code&gt;unchecked operator&lt;/code&gt; as an explicit counterpart to &lt;code&gt;checked operator&lt;/code&gt;?
Doing so would simplify the experience for users wanting to explicitly differentiate the two kinds at a glance and would allow a nesting level to be removed if the default context for the compilation is &lt;code&gt;checked&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Should the language allow &lt;code&gt;checked&lt;/code&gt; and &lt;code&gt;unchecked&lt;/code&gt; modifiers on other methods (e.g. &lt;code&gt;static checked void M()&lt;/code&gt;)?
This would allow removing nesting levels for methods that require it but, unlike with operators, would not modify the emitted name for the method.&lt;/p&gt;
&lt;p&gt;Should the language require the &lt;code&gt;checked&lt;/code&gt; (and possibly &lt;code&gt;unchecked&lt;/code&gt;) modifier(s) to appear directly before the &lt;code&gt;operator&lt;/code&gt; keyword or should they be allowed anywhere in the list of modifiers?&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Fri, 16 Apr 2021 23:28:20 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4651</guid>
      <link>https://github.com/dotnet/csharplang/issues/4651</link>
      <title>Issue: [Proposal]: Explicit Field Layout Improvements - @timcassell</title>
      <description>&lt;h1&gt;Explicit Field Layout Improvements&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Expand explicit field layouts to allow native word-size offsets and automatic offsets for unknown field size.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Currently, because &lt;code&gt;FieldOffsetAttribute&lt;/code&gt; only allows you to specify the offset in bytes, if you have more than 1 reference field, you have to choose between only supporting 32-bit runtime by offsetting reference fields by multiples of 4 bytes, always offset reference fields by multiples of 8 bytes, wasting space in 32-bit runtimes, or don't use explicit layout.&lt;/p&gt;
&lt;p&gt;The C# 9.0 feature for native sized integers &lt;code&gt;nint&lt;/code&gt;/&lt;code&gt;nuint&lt;/code&gt; further creates this need for explicit layout improvements to be as efficient as possible.&lt;/p&gt;
&lt;p&gt;Additionally, explicit layouts are currently impossible with generic fields where the generics can be structs of unknown size (unless there's only 1 and it's placed after all other fields).&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;For offsets by native word-size, a new &lt;code&gt;FieldOffsetAttribute&lt;/code&gt; constructor will be needed: &lt;code&gt;public FieldOffsetAttribute(int offsetWords, int offsetBytes)&lt;/code&gt; where the actual offset is determined by the runtime by calculating &lt;code&gt;IntPtr.Size() * offsetWords + offsetBytes&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;[StructLayout(LayoutKind.Explicit)]
public struct M
{
    [FieldOffset(0)] // offset 0
    public object obj;
    [FieldOffset(1, 0)] // offset by 1 word
    public nint num1;
    [FieldOffset(2, 0)] // offset by 2 words
    public byte num2;
    [FieldOffset(2, 2)] // offset by 2 words + 2 bytes - adds 1 byte padding before this
    public short num3;
    [FieldOffset(2, 4)] // offset by 2 words + 4 bytes
    public byte num4;
    // remaining 3 bytes are padded.
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;sizeof(M)&lt;/code&gt; should be 16 in 32-bit runtime and 24 in 64-bit runtime.&lt;/p&gt;
&lt;p&gt;--&lt;/p&gt;
&lt;p&gt;For auto layout fields, a new &lt;code&gt;FieldOffsetAttribute&lt;/code&gt; constructor could be made that accepts a new enum containing only a single option: &lt;code&gt;public FieldOffsetAttribute(OffsetType offsetType)&lt;/code&gt;. The runtime would decide how to offset these fields based on the type's size.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;[StructLayout(LayoutKind.Explicit)]
public struct M&amp;lt;T1, T2&amp;gt;
{
    [FieldOffset(0)] // offset 0
    public object obj;
    [FieldOffset(1, 0)] // offset by 1 word
    public short num1;
    [FieldOffset(1, 2)] // offset by 1 word + 2 bytes
    public byte num2;
    [FieldOffset(OffsetType.Auto)] // auto offset
    public T1 t1;
    [FieldOffset(OffsetType.Auto)] // auto offset
    public T2 t2;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;sizeof(M&amp;lt;byte, nuint&amp;gt;)&lt;/code&gt; should be 12 in 32-bit runtime and 24 in 64-bit runtime.
&lt;code&gt;sizeof(M&amp;lt;object, short&amp;gt;)&lt;/code&gt; should be 16 in 32-bit runtime and 24 in 64-bit runtime.
&lt;code&gt;sizeof(M&amp;lt;nint, long&amp;gt;)&lt;/code&gt; should be 20 in 32-bit runtime and 32 in 64-bit runtime.&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;None.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;None.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;Should we use &lt;code&gt;FieldOffset(OffsetType.Auto)&lt;/code&gt; or a new attribute like &lt;code&gt;FieldOffsetAuto&lt;/code&gt;?&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
</description>
      <pubDate>Wed, 14 Apr 2021 04:08:51 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4629</guid>
      <link>https://github.com/dotnet/csharplang/issues/4629</link>
      <title>Issue: Proposal: "Opaque" parameters - @agocke</title>
      <description>&lt;h1&gt;Opaque parameters&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;This proposal would include a new syntax form for parameter declarations: an optional &lt;code&gt;some&lt;/code&gt;
contextual keyword that precedes the parameter type, similar to &lt;code&gt;ref&lt;/code&gt; or &lt;code&gt;params&lt;/code&gt;. This would
look like:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;void M(some IEnumerable&amp;lt;int&amp;gt; e) { ... }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The parameter type would be required to be an interface. The semantics would be that the
parameter type is not the interface itself, but an unspeakable generic type constrained to the
interface listed as the parameter type, e.g.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;void M&amp;lt;T&amp;gt;(T e) where T : IEnumerable&amp;lt;int&amp;gt; { ... }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;On the caller side, the call would look like &lt;code&gt;List&amp;lt;int&amp;gt; x = ...; M(x)&lt;/code&gt;. From the caller's perspective,
there would be no generic parameters to this method. The compiler would be required to infer
the appropriate generic parameter type, which should be the exact type of the argument.&lt;/p&gt;
&lt;p&gt;Overloading:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;some&lt;/code&gt; parameters should be valid as overloads, similar to &lt;code&gt;ref&lt;/code&gt; params, and should be considered a better candidate than a parameter of the same type without &lt;code&gt;some&lt;/code&gt;. This would allow for the following overload scenario:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-C#"&gt;void M(some IEnumerable&amp;lt;int&amp;gt; e) { ... }
void M(IEnumerable&amp;lt;int&amp;gt; e) { ... }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Users could then decide to add &lt;code&gt;some&lt;/code&gt; overloads that would have improved performance and be preferred when present.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;This addresses a couple problems. First, it's very verbose to declare a signature of the above form,
although it's often very desirable for performance. Second, it hides irrelevant information (the generic
parameter name), which is not useful aside from the performance implications of the definition. Third,
due to the C# type inference algorithm, the verbose equivalent is often very difficult for the compiler
to infer. Because the parameter is not referenceable anywhere else in the parameter list, it should be
easy to infer the correct parameter type here.&lt;/p&gt;
&lt;p&gt;Aside: credit to Swift for inspiration here, although the problems aren't the same.&lt;/p&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;Extra syntax, more complexity.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;TBD&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;Exact lowering and inference. A compiler-recognized attribute on the generated type parameters to remove
them from the list of user-visible type parameters may be necessary, along with a modopt on the &lt;code&gt;some&lt;/code&gt; parameters
to enable overloading.&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
</description>
      <pubDate>Wed, 07 Apr 2021 18:34:23 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4602</guid>
      <link>https://github.com/dotnet/csharplang/issues/4602</link>
      <title>Issue: [Proposal]: record-like constructor syntax for classes - @wizofaus</title>
      <description>&lt;p&gt;I have quite a number of classes in my codebase that essentially look like:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Foo /* : IFoo */
{
    public Foo(Bar bar, WidgetService service, ILogger logger)
    {
        this.bar = bar;
        this.service = service;
        this.logger = logger;
    }

    /* public functions etc. */
 
    readonly Bar bar;
    readonly WidgetService service;
    readonly Logger logger;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Essentially the class has no state of its own and all dependencies are passed into the constructor.  But that means quite a lot of boilerplate code.  It can be removed by changing it to a record:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;record Foo(Bar bar, WidgetService service, ILogger logger)
{  
    /* public functions etc. */
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;And I do like how clean it looks - but at the expense of the fact that what were private fields are now public (but still read-only) properties, and that whatever Foo might actually be, a &amp;quot;Record&amp;quot; is a poor description of it.&lt;/p&gt;
&lt;p&gt;So is there any way I can achieve similarly clean code free of boilerplate but keep it as a class with private/internal fields?  If not, could there be a reason C# doesn't support this?&lt;/p&gt;
</description>
      <pubDate>Mon, 29 Mar 2021 09:57:55 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4587</guid>
      <link>https://github.com/dotnet/csharplang/issues/4587</link>
      <title>Issue: [Proposal]: nested members in `with` and object creation - @jcouv</title>
      <description>&lt;h1&gt;nested members in &lt;code&gt;with&lt;/code&gt; and object creation&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;If we go ahead and add support for &lt;a href="https://github.com/dotnet/csharplang/issues/4394"&gt;extended names in property patterns&lt;/a&gt; (&lt;code&gt;if (e is MethodCallExpression { Method.Name: &amp;quot;MethodName&amp;quot; })&lt;/code&gt;), it makes sense for symmetry to allow this in &lt;code&gt;with&lt;/code&gt; and object creation expressions:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;methodCallExpression with { Method.Name = &amp;quot;MethodName&amp;quot; }&lt;/code&gt;
&lt;code&gt;new MethodCallExpression(...) { Method.Name = &amp;quot;MethodName&amp;quot; }&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;This is particularly useful in the &lt;code&gt;with&lt;/code&gt; expression, so you don't have to write:
&lt;code&gt;methodCallExpression with { Method = methodCallExpression.Method with { Name = &amp;quot;MethodName&amp;quot; } }&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
</description>
      <pubDate>Fri, 26 Mar 2021 22:01:28 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4600</guid>
      <link>https://github.com/dotnet/csharplang/issues/4600</link>
      <title>Issue: Linq SelectMany yield return - @denisenko93</title>
      <description>&lt;p&gt;Allow use of yield return in lambda functions.&lt;/p&gt;
&lt;p&gt;Example:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class Client
{
   public string Phone1 {get;set;}
   public string Phone2 {get;set;}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Does not works:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var phones = clients.SelectMany(x =&amp;gt;
{
   yield return x.Phone1;
   yield return x.Phone2;
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Only methods, operators and accessors could contain 'yield' statement&lt;/p&gt;
&lt;p&gt;Works:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var phones = clients.SelectMany(GetPhones);

IEnumerable&amp;lt;string&amp;gt; GetPhones(Client client)
{
   yield return client.Phone1;
   yield return client.Phone2;
}
&lt;/code&gt;&lt;/pre&gt;
</description>
      <pubDate>Thu, 25 Mar 2021 10:22:04 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4578</guid>
      <link>https://github.com/dotnet/csharplang/issues/4578</link>
      <title>Issue: Proposal: Backtick as Alternative Double Quote Syntax - @dhughes55</title>
      <description>&lt;!--
New language feature proposals should fully fill out this template. This should include a complete detailed design, which describes the syntax of the feature, what that syntax means, and how it affects current parts of the spec. Please make sure to point out specific spec sections that need to be updated for this feature. If you do not have the knowledge or experience to fill this out completely, that's perfectly alright: please open this proposal as a Discussion instead (https://github.com/dotnet/csharplang/discussions/new) and the community can generally discuss the proposal in less formal terms.
--&gt;
&lt;h1&gt;Backtick as Alternative Double Quote Syntax&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Currently in the C# programming language, the only way to specify a string literal is to use double quotes.  I think there should be a second option for declaring a string literal: the backtick.  JavaScript supports the backtick as part of its template literal implementation.  I think C# should allow something similar, but simpler.  Simply allow the backtick to be treated as an alternative to the double quote character when declaring a string literal.&lt;/p&gt;
&lt;p&gt;Example:
var string1 = &amp;quot;This is the only way to write a string right now.&amp;quot;
var string2 = `With this feature, this would now be an acceptable alternative.`&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Who has ever needed to write HTML inside a string?  Do any of these three examples look familiar to anyone?
var html = &amp;quot;&amp;lt;span class=\&amp;quot;icon phone\&amp;quot;&amp;gt;Content&amp;lt;/span&amp;gt;&amp;quot;;
var html = &amp;quot;&amp;lt;span class='icon phone'&amp;gt;Content&amp;lt;/span&amp;gt;&amp;quot;;
var html = @&amp;quot;&amp;lt;span class=&amp;quot;&amp;quot;icon phone&amp;quot;&amp;quot;&amp;gt;Content&amp;lt;/span&amp;gt;&amp;quot;;&lt;/p&gt;
&lt;p&gt;All three are theoretically usable but each has its own flaws.  Wouldn't it be nice (and much more elegant) to be able to write this instead?
var html = `&amp;lt;span class=&amp;quot;icon phone&amp;quot;&amp;gt;Content&amp;lt;/span&amp;gt;`;&lt;/p&gt;
&lt;!-- Why are we doing this? What use cases does it support? What is the expected outcome? --&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;The main use case is providing a way to not have to constantly escape string literals.  There's not much to explain in terms of the usage. The back tick would simply be treated as an alternative syntax to the double quote in pretty much any place where double quotes are allowed in the creation of string literals (with the exception of now allowing @`string` syntax.  This means you could do things like below:&lt;/p&gt;
&lt;p&gt;var foo = $`My name is {firstName}`;&lt;/p&gt;
&lt;p&gt;But you couldn't do the following since the compiler wouldn't know whether to treat back-to-back double quotes as one or two quotes:&lt;/p&gt;
&lt;p&gt;var bar = @`This however, wouldn't be allowed since it points to two different syntax techniques.`&lt;/p&gt;
&lt;!-- This is the bulk of the proposal. Explain the design in enough detail for somebody familiar with the language to understand, and for somebody familiar with the compiler to implement, and include examples of how the feature is used. Please include syntax and desired semantics for the change, including linking to the relevant parts of the existing C# spec to describe the changes necessary to implement this feature. An initial proposal does not need to cover all cases, but it should have enough detail to enable a language team member to bring this proposal to design if they so choose. --&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;p&gt;I can't think of any good reason.  I'm not a C# expert by any means, but to my knowledge the backtick isn't reserved for any other special purpose.  That said, it's hard to imagine that this is the first time anyone has suggested this feature since there's something similar in JavaScript.  So some drawbacks may already be out there and I just don't know about them.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;p&gt;There's no real negative impact of not doing this other than code continuing to sometimes be less legible than it could be.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Wed, 24 Mar 2021 02:16:36 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4576</guid>
      <link>https://github.com/dotnet/csharplang/issues/4576</link>
      <title>Issue: [Proposal]: [Dedicated case for invalid value in the switch statement and expression] - @tymtam2</title>
      <description>&lt;h1&gt;Dedicated case for invalid value in the switch statement and expression&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Introduce special cases for the swtich statement and switch expression (&lt;code&gt;unmapped&lt;/code&gt; and &lt;code&gt;?&lt;/code&gt; respectively) for enums
to
handle unmapped values (as opposed to all &amp;quot;other mapped values&amp;quot;)
in order to
keep compile time ability to detect unhandled known values
because
the current &lt;code&gt;default&lt;/code&gt;/&lt;code&gt;_&lt;/code&gt; solution removes compile time detection of known unhandled valus in order to handle the fact that c# enums can have invalid values.&lt;/p&gt;
&lt;p&gt;For example:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public enum Directions { Up, Down, Right, Left }
    
(...)    
var o = direction switch
{
     Directions.Up    =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Down =&amp;gt; &amp;quot;vertical&amp;quot;,
     _ =&amp;gt; &amp;quot;horizontal&amp;quot;,
     ? =&amp;gt; throw new Exception($&amp;quot;Invalid value for direction: {direction}&amp;quot;);
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Enums in C# are special. A variable of an enum type can hold a value that are no mapped in the enum declaration. The switch expression's and switch statement's abiltiy to detect errors at compile time is compromised because developers end up adding &lt;code&gt;_&lt;/code&gt; or &lt;code&gt;default&lt;/code&gt; to handle umapped values and loose the compile time warning about unhandled mapped values. Providing a dedicated case for unhandled values will create a safer, more correct code.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;Introduce &lt;code&gt;unmapped&lt;/code&gt; for the switch statement and &lt;code&gt;?&lt;/code&gt; for the switch expression applicable when switching on an enum. The &lt;code&gt;unmapped&lt;/code&gt;/&lt;code&gt;_&lt;/code&gt; case is invoked when the examined value is not mapped.&lt;/p&gt;
&lt;p&gt;Let's consider the following setup.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public enum Directions { Up, Down, Right, Left }
    
(...)    
var o = direction switch
{
     Directions.Up    =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Down =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Right    =&amp;gt; &amp;quot;horizonal&amp;quot;,
     Directions.Left =&amp;gt; &amp;quot;horizontal&amp;quot;,
     ? =&amp;gt; throw new Exception($&amp;quot;Invalid value for direction: {direction}&amp;quot;);
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If &lt;code&gt;direction&lt;/code&gt; is (Directions)5 then &lt;code&gt;?&lt;/code&gt; is invoked. If the value is &lt;code&gt;Directions.Left&lt;/code&gt; then &lt;code&gt;_&lt;/code&gt; is invoked.&lt;/p&gt;
&lt;p&gt;If &lt;code&gt;?&lt;/code&gt; is not provided the &lt;code&gt;_&lt;/code&gt; is invoked.&lt;/p&gt;
&lt;p&gt;The primary use case is keeping the ability to detect unhandled cases when adding new values to enum.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public enum Directions { Up, Down, Right, Left, Forward, Back  } 
    
(...)    
var o = direction switch
{
     Directions.Up    =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Down =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Right    =&amp;gt; &amp;quot;horizonal&amp;quot;,
     Directions.Left =&amp;gt; &amp;quot;horizontal&amp;quot;,
     ? =&amp;gt; throw new Exception($&amp;quot;Invalid value for direction: {direction}&amp;quot;);
     // Compile time warning: Forward, Back are not handled 
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Currently the default handler the error of missing handlers would not be detected until runtime:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public enum Directions { Up, Down, Right, Left, Forward, Back  }
    
(...)    
var o = direction switch
{
     Directions.Up    =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Down =&amp;gt; &amp;quot;vertical&amp;quot;,
     Directions.Right    =&amp;gt; &amp;quot;horizonal&amp;quot;,
     Directions.Left =&amp;gt; &amp;quot;horizontal&amp;quot;,
     _ =&amp;gt; throw new Exception($&amp;quot;Invalid value for direction: {direction}&amp;quot;); // Only at runtime. So late!
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;p&gt;Code is not backward compatible. The code may slow down.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;Allow first calls, in code, acknowledment of
&lt;code&gt;Warning CS8509 The switch expression does not handle all possible values of its input type (it is not exhaustive). For example, the pattern '...' is not covered.&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Tue, 23 Mar 2021 06:00:56 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4568</guid>
      <link>https://github.com/dotnet/csharplang/issues/4568</link>
      <title>Issue: Allow a lambda expression with a statement body to be converted to an expression tree - @maurosampietro</title>
      <description>&lt;p&gt;Error CS0834 says: A lambda expression with a statement body cannot be converted to an expression tree.&lt;/p&gt;
&lt;p&gt;So this is possible:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;Expression&amp;lt;Func&amp;lt;string, bool&amp;gt;&amp;gt; example1 = str =&amp;gt; str == &amp;quot;ok&amp;quot;;`
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;But this is not possible:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;    Expression&amp;lt;Func&amp;lt;string, bool&amp;gt;&amp;gt; example2 = str =&amp;gt; 
    { 
         if( str == &amp;quot;ok&amp;quot; ) 
             return true; 

         return false
    };
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;However creating a new method and passing in the params does the trick:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c#"&gt;    Expression&amp;lt;Func&amp;lt;string, bool&amp;gt;&amp;gt; example3 = str =&amp;gt; RelayMethod( str );

    private bool RelayMethod( string str )
    {
        if( str == &amp;quot;ok&amp;quot; )
            return true;

        return false;
    }

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The trick always works in any scenario so why don't we automatically generate the 'method trick' (when a lambda expression with a statement body is detected) and remove this limit?&lt;/p&gt;
</description>
      <pubDate>Sun, 21 Mar 2021 12:06:58 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4565</guid>
      <link>https://github.com/dotnet/csharplang/issues/4565</link>
      <title>Issue: [Proposal]: Cancel Async Functions Without Throw - @timcassell</title>
      <description>&lt;h1&gt;Cancel Async Functions Without Throw&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Cancelation of async functions is currently only possible by throwing an &lt;code&gt;OperationCanceledException&lt;/code&gt;. This proposal allows canceling async functions directly via awaiters implementing an &lt;code&gt;IsCanceled&lt;/code&gt; property and AsyncMethodBuilders implementing a &lt;code&gt;void SetCanceled&amp;lt;TAwaiter&amp;gt;(ref TAwaiter)&lt;/code&gt; method. Finally blocks are allowed to run after an awaiter is canceled, just like if an exception were thrown.&lt;/p&gt;
&lt;p&gt;&lt;a href="https://github.com/dotnet/csharplang/discussions/4450"&gt;Previous Discussion&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Performance! Benchmarks show up to 1000x performance improvement when canceling directly instead of throwing an exception. &lt;a href="https://github.com/timcassell/CancelAsyncBenchmark"&gt;Source&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;|              Method | Recursion |          Mean |        Error |       StdDev |    Ratio | RatioSD |  Gen 0 | Gen 1 | Gen 2 | Allocated |
|-------------------- |---------- |--------------:|-------------:|-------------:|---------:|--------:|-------:|------:|------:|----------:|
|   DirectCancelation |         5 |      66.53 ns |     0.193 ns |     0.161 ns |     1.00 |    0.00 |      - |     - |     - |         - |
| ThrowNewCancelation |         5 |  70,834.43 ns |   451.118 ns |   399.905 ns | 1,065.02 |    6.72 | 0.3662 |     - |     - |    1248 B |
|  RethrowCancelation |         5 |  66,003.14 ns |   613.233 ns |   543.615 ns |   992.29 |    8.66 | 0.1221 |     - |     - |     568 B |
|                     |           |               |              |              |          |         |        |       |       |           |
|   DirectCancelation |        10 |     122.94 ns |     0.063 ns |     0.053 ns |     1.00 |    0.00 |      - |     - |     - |         - |
| ThrowNewCancelation |        10 | 128,770.42 ns | 1,330.938 ns | 1,244.960 ns | 1,046.39 |   10.72 | 0.4883 |     - |     - |    2288 B |
|  RethrowCancelation |        10 | 119,599.25 ns |   695.557 ns |   616.593 ns |   972.49 |    5.02 | 0.2441 |     - |     - |     928 B |
|                     |           |               |              |              |          |         |        |       |       |           |
|   DirectCancelation |        20 |     320.03 ns |     0.403 ns |     0.377 ns |     1.00 |    0.00 |      - |     - |     - |         - |
| ThrowNewCancelation |        20 | 249,736.53 ns | 2,580.620 ns | 2,413.913 ns |   780.35 |    7.51 | 0.9766 |     - |     - |    4368 B |
|  RethrowCancelation |        20 | 227,962.82 ns | 2,017.599 ns | 1,887.263 ns |   712.32 |    5.93 | 0.4883 |     - |     - |    1648 B |
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;Any awaiter can implement the &lt;code&gt;bool IsCanceled { get; }&lt;/code&gt; property to enable fast-tracked async cancelations. The compiler could use duck-typing to check for the property (like it does with &lt;code&gt;GetResult&lt;/code&gt;), or a new interface (like it does with &lt;code&gt;I(Critical)NotifyCompletion&lt;/code&gt;).&lt;/p&gt;
&lt;p&gt;AsyncMethodBuilders will need to add a new method &lt;code&gt;void SetCanceled&amp;lt;TAwaiter&amp;gt;(ref TAwaiter awaiter)&lt;/code&gt; to enable fast-tracked cancelations for the async return type, where &lt;code&gt;awaiter&lt;/code&gt; is the awaiter whose &lt;code&gt;IsCanceled&lt;/code&gt; property returned &lt;code&gt;true&lt;/code&gt;. Both the awaiter and the builder must have those methods in order for the compiler to emit the fast-tracked cancelation instructions, otherwise it falls back to the existing implementation.&lt;/p&gt;
&lt;p&gt;Tasks won't be using the &lt;code&gt;ref TAwaiter awaiter&lt;/code&gt;, it will just set the task to canceled. The purpose of the &lt;code&gt;ref TAwaiter awaiter&lt;/code&gt; is for custom task-like types to be able to accept custom cancelations.&lt;/p&gt;
&lt;p&gt;Example state-machine outputs:&lt;/p&gt;
&lt;details&gt;&lt;summary&gt;CancellationToken and Task extensions:&lt;/summary&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;public static class CancellationTokenExtensions
{
    public readonly struct CancelAsyncAwaitable : INotifyCompletion
    {
        private readonly CancellationToken _token;
        
        public CancelAsyncAwaitable(CancellationToken token) =&amp;gt; _token = token;
        public CancelAsyncAwaitable GetAwaiter() =&amp;gt; this;
        public bool IsCompleted =&amp;gt; true;
        public bool IsCanceled =&amp;gt; _token.IsCancellationRequested;
        public void GetResult() =&amp;gt; _token.ThrowIfCancellationRequested();
        void INotifyCompletion.OnCompleted(Action continuation) =&amp;gt; throw new NotImplementedException();   
    }
    
    public static CancelAsyncAwaitable CancelAsyncIfCancellationRequested(this CancellationToken token)
    {
        return new CancelAsyncAwaitable(token);   
    }
}

public static class TaskExtensions
{
    public readonly struct CancelAsyncAwaitable : ICriticalNotifyCompletion
    {
        private readonly Task _task;
        private readonly TaskAwaiter _awaiter;
        
        public CancelAsyncAwaitable(Task task)
        {
            _task = task;
            _awaiter = task.GetAwaiter();
        }
        public CancelAsyncAwaitable GetAwaiter() =&amp;gt; this;
        public bool IsCompleted =&amp;gt; _awaiter.IsCompleted;
        public bool IsCanceled =&amp;gt; _task.Status == TaskStatus.Canceled;
        public void GetResult() =&amp;gt; _awaiter.GetResult();
        public void OnCompleted(Action continuation) =&amp;gt; _awaiter.OnCompleted(continuation);
        public void UnsafeOnCompleted(Action continuation) =&amp;gt; _awaiter.UnsafeOnCompleted(continuation);
    }
    
    public static CancelAsyncAwaitable CancelAsyncIfCanceled(this Task task)
    {
        return new CancelAsyncAwaitable(task);   
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/details&gt;
&lt;p&gt;Simple Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;public class M
{
    async Task&amp;lt;int&amp;gt; FuncAsync(CancellationToken token)
    {
        await Task.Yield();
        await token.CancelAsyncIfCancellationRequested();
        return 42;
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;details&gt;&lt;summary&gt;Equivalent C#:&lt;/summary&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;public class M
{
    [StructLayout(LayoutKind.Auto)]
    [CompilerGenerated]
    private struct _d__0 : IAsyncStateMachine
    {
        public int _1__state;

        public AsyncTaskMethodBuilder&amp;lt;int&amp;gt; _t__builder;

        public CancellationToken token;

        private YieldAwaitable.YieldAwaiter _u__1;

        private CancellationTokenExtensions.CancelAsyncAwaitable _u__2;

        private void MoveNext()
        {
            int num = _1__state;
            int result;
            try
            {
                CancellationTokenExtensions.CancelAsyncAwaitable awaiter;
                YieldAwaitable.YieldAwaiter awaiter2;
                if (num != 0)
                {
                    if (num == 1)
                    {
                        awaiter = _u__2;
                        _u__2 = default(CancellationTokenExtensions.CancelAsyncAwaitable);
                        num = (_1__state = -1);
                        goto IL_00cb;
                    }
                    awaiter2 = Task.Yield().GetAwaiter();
                    if (!awaiter2.IsCompleted)
                    {
                        num = (_1__state = 0);
                        _u__1 = awaiter2;
                        _t__builder.AwaitUnsafeOnCompleted(ref awaiter2, ref this);
                        return;
                    }
                }
                else
                {
                    awaiter2 = _u__1;
                    _u__1 = default(YieldAwaitable.YieldAwaiter);
                    num = (_1__state = -1);
                }
                awaiter2.GetResult();
                awaiter = CancellationTokenExtensions.CancelAsyncIfCancellationRequested(token).GetAwaiter();
                if (!awaiter.IsCompleted)
                {
                    num = (_1__state = 1);
                    _u__2 = awaiter;
                    _t__builder.AwaitOnCompleted(ref awaiter, ref this);
                    return;
                }
                goto IL_00cb;
                IL_00cb:
                if (awaiter.IsCanceled)
                {
                    _1__state = -2;
                    _t__builder.SetCanceled(ref awaiter);
                    return;
                }
                awaiter.GetResult();
                result = 42;
            }
            catch (Exception exception)
            {
                _1__state = -2;
                _t__builder.SetException(exception);
                return;
            }
            _1__state = -2;
            _t__builder.SetResult(result);
        }

        void IAsyncStateMachine.MoveNext()
        {
            //ILSpy generated this explicit interface implementation from .override directive in MoveNext
            this.MoveNext();
        }

        [DebuggerHidden]
        private void SetStateMachine(IAsyncStateMachine stateMachine)
        {
            _t__builder.SetStateMachine(stateMachine);
        }

        void IAsyncStateMachine.SetStateMachine(IAsyncStateMachine stateMachine)
        {
            //ILSpy generated this explicit interface implementation from .override directive in SetStateMachine
            this.SetStateMachine(stateMachine);
        }
    }

    [AsyncStateMachine(typeof(_d__0))]
    private Task&amp;lt;int&amp;gt; FuncAsync(CancellationToken token)
    {
        _d__0 stateMachine = default(_d__0);
        stateMachine._t__builder = AsyncTaskMethodBuilder&amp;lt;int&amp;gt;.Create();
        stateMachine.token = token;
        stateMachine._1__state = -1;
        stateMachine._t__builder.Start(ref stateMachine);
        return stateMachine._t__builder.Task;
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/details&gt;
&lt;p&gt;Complex Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;public class M
{
    async Task&amp;lt;int&amp;gt; FuncAsync(CancellationToken token)
    {
        try
        {
            await OtherFuncAsync().CancelAsyncIfCanceled();
            await token.CancelAsyncIfCancellationRequested();
            return 42;
        }
        finally
        {
            await Task.Delay(1);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;details&gt;&lt;summary&gt;Equivalent C#:&lt;/summary&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;public class M
{
    [StructLayout(LayoutKind.Auto)]
    [CompilerGenerated]
    private struct _d__0 : IAsyncStateMachine
    {
        public int _1__state;

        public AsyncTaskMethodBuilder&amp;lt;int&amp;gt; _t__builder;

        public M _4__this;

        public CancellationToken token;

        private object _7__wrap1;

        private int _7__wrap2;

        private int _7__wrap3;

        private TaskExtensions.CancelAsyncAwaitable _u__1;

        private CancellationTokenExtensions.CancelAsyncAwaitable _u__2;

        private TaskAwaiter _u__3;

        private int _1__canceler;

        private void MoveNext()
        {
            int num = _1__state;
            M m = _4__this;
            int result = default(int);
            try
            {
                TaskAwaiter awaiter;
                if ((uint)num &amp;gt; 1u)
                {
                    if (num == 2)
                    {
                        awaiter = _u__3;
                        _u__3 = default(TaskAwaiter);
                        num = (_1__state = -1);
                        goto IL_017b;
                    }
                    _7__wrap1 = null;
                    _7__wrap2 = 0;
                }
                object obj2;
                try
                {
                    CancellationTokenExtensions.CancelAsyncAwaitable awaiter2;
                    TaskExtensions.CancelAsyncAwaitable awaiter3;
                    if (num != 0)
                    {
                        if (num == 1)
                        {
                            awaiter2 = _u__2;
                            _u__2 = default(CancellationTokenExtensions.CancelAsyncAwaitable);
                            num = (_1__state = -1);
                            goto IL_0100;
                        }
                        awaiter3 = TaskExtensions.CancelAsyncIfCanceled(m.FuncAsync(default(CancellationToken))).GetAwaiter();
                        if (!awaiter3.IsCompleted)
                        {
                            num = (_1__state = 0);
                            _u__1 = awaiter3;
                            _t__builder.AwaitUnsafeOnCompleted(ref awaiter3, ref this);
                            return;
                        }
                    }
                    else
                    {
                        awaiter3 = _u__1;
                        _u__1 = default(TaskExtensions.CancelAsyncAwaitable);
                        num = (_1__state = -1);
                    }
                    if (awaiter3.IsCanceled)
                    {
                        _u__1 = awaiter3;
                        _canceled_awaiter = 1;
                        goto IL_finally;
                    }
                    awaiter3.GetResult();
                    awaiter2 = CancellationTokenExtensions.CancelAsyncIfCancellationRequested(token).GetAwaiter();
                    if (!awaiter2.IsCompleted)
                    {
                        num = (_1__state = 1);
                        _u__2 = awaiter2;
                        _t__builder.AwaitOnCompleted(ref awaiter2, ref this);
                        return;
                    }
                    goto IL_0100;
                    IL_0100:
                    if (awaiter2.IsCanceled)
                    {
                        _u__2 = awaiter2;
                        _canceled_awaiter = 2;
                        goto IL_finally;
                    }
                    awaiter2.GetResult();
                    _7__wrap3 = 42;
                    _7__wrap2 = 1;
                }
                catch (object obj)
                {
                    obj2 = (_7__wrap1 = obj);
                }
                IL_finally:
                awaiter = Task.Delay(1).GetAwaiter();
                if (!awaiter.IsCompleted)
                {
                    num = (_1__state = 2);
                    _u__3 = awaiter;
                    _t__builder.AwaitUnsafeOnCompleted(ref awaiter, ref this);
                    return;
                }
                goto IL_017b;
                IL_017b:
                awaiter.GetResult();
                obj2 = _7__wrap1;
                if (obj2 != null)
                {
                    Exception obj3 = obj2 as Exception;
                    if (obj3 == null)
                    {
                        throw obj2;
                    }
                    ExceptionDispatchInfo.Capture(obj3).Throw();
                }
                int num2 = _7__wrap2;
                if (num2 == 1)
                {
                    result = _7__wrap3;
                }
                else
                {
                    _7__wrap1 = null;
                }
            }
            catch (Exception exception)
            {
                _1__state = -2;
                _t__builder.SetException(exception);
                return;
            }
            _1__state = -2;
            int canceled = _1__canceler;
            switch (canceled)
            {
                case 1:
                {
                    _t__builder.SetCanceled(ref _u__1);
                    return;
                }
                case 2:
                {
                    _t__builder.SetCanceled(ref _u__2);
                    return;
                }
            }
            _t__builder.SetResult(result);
        }

        void IAsyncStateMachine.MoveNext()
        {
            //ILSpy generated this explicit interface implementation from .override directive in MoveNext
            this.MoveNext();
        }

        [DebuggerHidden]
        private void SetStateMachine(IAsyncStateMachine stateMachine)
        {
            _t__builder.SetStateMachine(stateMachine);
        }

        void IAsyncStateMachine.SetStateMachine(IAsyncStateMachine stateMachine)
        {
            //ILSpy generated this explicit interface implementation from .override directive in SetStateMachine
            this.SetStateMachine(stateMachine);
        }
    }

    [AsyncStateMachine(typeof(_d__0))]
    private Task&amp;lt;int&amp;gt; FuncAsync(CancellationToken token)
    {
        _d__0 stateMachine = default(_d__0);
        stateMachine._t__builder = AsyncTaskMethodBuilder&amp;lt;int&amp;gt;.Create();
        stateMachine._4__this = this;
        stateMachine.token = token;
        stateMachine._1__state = -1;
        stateMachine._t__builder.Start(ref stateMachine);
        return stateMachine._t__builder.Task;
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/details&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;Unlike exceptions, direct cancelations cannot be caught. Therefore, existing types should use the existing functionality by default, and expose new APIs for performance.&lt;/p&gt;
&lt;p&gt;More work for the compiler.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;New keywords - undesirable and unlikely to get implemented for such a niche feature.&lt;/p&gt;
&lt;p&gt;Awaiters have a &lt;code&gt;GetException&lt;/code&gt; method and the state-machine checks the return value for null - Still requires allocating an exception object and brings up the question of how the async stack traces are preserved.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;Should a new keyword be added to catch these direct cancelations? (I think no)&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
</description>
      <pubDate>Sun, 21 Mar 2021 04:38:05 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4544</guid>
      <link>https://github.com/dotnet/csharplang/issues/4544</link>
      <title>Issue: [Proposal]: abstract enumerations - @Youssef1313</title>
      <description>&lt;h1&gt;abstract enumerations&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;Allows for multiple enumerations to share common members.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;(excuse me for the example being very specific to Roslyn codebase)&lt;/p&gt;
&lt;p&gt;This reduces extra clutter when enumerations are supposed to share common members (as an example, CodeAnalysis.CSharp.SyntaxKind and CodeAnalysis.VisualBasic.SyntaxKind). The current way to do things is to have a base class with abstract method, for example, &lt;code&gt;IsIdentifier&lt;/code&gt;, where both overrides just do the same thing, &lt;code&gt;return node.IsKind(SyntaxKind.IdentifierToken)&lt;/code&gt;. This proposal removes this extra clutter by having a new concept, abstract enumerations.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;Changes the syntax to:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-antlr"&gt;enum_declaration
  : attribute_list* modifier* 'abstract'? 'enum' identifier_token base_list? '{' (enum_member_declaration (',' enum_member_declaration)* ','?)? '}' ';'?
  ;
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;An abstract enum should be &lt;em&gt;closed&lt;/em&gt;. i.e, a parameter of type &lt;code&gt;MyAbstractEnum&lt;/code&gt; cannot be passed &lt;code&gt;(MyAbstractEnum)0&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;A member of abstract enum &lt;strong&gt;cannot&lt;/strong&gt; be assigned a value.&lt;/li&gt;
&lt;li&gt;A concrete enum should re-define all members in the abstract enum so each member is clear its value.&lt;/li&gt;
&lt;li&gt;A concrete enum may not derive from multiple enumerations, nor have a base type such as &lt;code&gt;byte&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;An abstract enum may have a base type such as &lt;code&gt;byte&lt;/code&gt;, which implies that all derived enums are also &lt;code&gt;byte&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;abstract enum BaseEnum { CommonMember }

enum NotDerived : byte { } // the same as what it does today.
enum NotAllowed : byte, BaseEnum { } // Not allowed.

enum Derived : BaseEnum { CommonMember, OtherExtraMember }
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Is this a common pattern enough to worth a new language feature?&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
</description>
      <pubDate>Tue, 16 Mar 2021 18:24:20 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4541</guid>
      <link>https://github.com/dotnet/csharplang/issues/4541</link>
      <title>Issue: [Proposal]: Allow use of return, break and continue with the null-coalescing operator (??) - @H4xX0r1337</title>
      <description>&lt;h1&gt;Allow use of return, break and continue with the null-coalescing operator (??)&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;The null-coalescing operator ?? allows throwing exceptions. I propose extending this feature to also support returning from a method and breaking and continuing from loops.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;Currently, only return is allowed. Also allowing the other keywords would make the language more consistent.
That would also allow more compact early returns in methods or loops with less boilerplate.&lt;/p&gt;
&lt;h2&gt;Example&lt;/h2&gt;
&lt;p&gt;IMHO, this way to read a file line-by-line would be the cleanest one.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-cs"&gt;using var reader = File.OpenText(&amp;quot;path/to/file.txt&amp;quot;);
while (true)
{
    var line = reader.ReadLine() ?? break; //exit loop if no line is available.
    Console.WriteLine(line);
}
&lt;/code&gt;&lt;/pre&gt;
</description>
      <pubDate>Tue, 16 Mar 2021 12:50:15 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4535</guid>
      <link>https://github.com/dotnet/csharplang/issues/4535</link>
      <title>Issue: [Proposal]: make EventHandler as a IList - @Banyc</title>
      <description>&lt;!--
New language feature proposals should fully fill out this template. This should include a complete detailed design, which describes the syntax of the feature, what that syntax means, and how it affects current parts of the spec. Please make sure to point out specific spec sections that need to be updated for this feature. If you do not have the knowledge or experience to fill this out completely, that's perfectly alright: please open this proposal as a Discussion instead (https://github.com/dotnet/csharplang/discussions/new) and the community can generally discuss the proposal in less formal terms.
--&gt;
&lt;h1&gt;Make &lt;code&gt;EventHandler&lt;/code&gt; as a &lt;code&gt;IList&lt;/code&gt;&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;!-- One paragraph explanation of the feature. --&gt;
&lt;p&gt;Please make &lt;code&gt;EventHandler&lt;/code&gt; as a &lt;code&gt;IList&lt;/code&gt; that could be &lt;code&gt;Clear()&lt;/code&gt;, &lt;code&gt;Add()&lt;/code&gt;, ...&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;!-- Why are we doing this? What use cases does it support? What is the expected outcome? --&gt;
&lt;p&gt;I want to clean up all the delegates that are previously assigned to a timer's &lt;code&gt;Elapsed&lt;/code&gt; event. I don't want it to fire the old delegates that have had the old parameters. I want the timer to fire the newest delegate with the newest parameters.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;!-- This is the bulk of the proposal. Explain the design in enough detail for somebody familiar with the language to understand, and for somebody familiar with the compiler to implement, and include examples of how the feature is used. Please include syntax and desired semantics for the change, including linking to the relevant parts of the existing C# spec to describe the changes necessary to implement this feature. An initial proposal does not need to cover all cases, but it should have enough detail to enable a language team member to bring this proposal to design if they so choose. --&gt;
&lt;p&gt;Just make something like:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;public class EventHandler : IList&amp;lt;Delegate&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;or&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;public class EventHandler : List&amp;lt;Delegate&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;!-- Why should we *not* do this? --&gt;
&lt;p&gt;Might take some time to implement and test, I guess?&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;!-- What other designs have been considered? What is the impact of not doing this? --&gt;
&lt;p&gt;There are a lot of complaints around this issue. One of the examples:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://stackoverflow.com/questions/91778/how-to-remove-all-event-handlers-from-an-event"&gt;https://stackoverflow.com/questions/91778/how-to-remove-all-event-handlers-from-an-event&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;It is not easy to remember/keep track of the old delegates that have been assigned to an event handler. Make &lt;code&gt;Clear()&lt;/code&gt; method could make life tons' better.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;!-- What parts of the design are still undecided? --&gt;
&lt;p&gt;Currently no.&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;!-- Link to design notes that affect this proposal, and describe in one sentence for each what changes they led to. --&gt;
&lt;p&gt;Currently no.&lt;/p&gt;
&lt;h2&gt;Relative on-going discussion&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://github.com/dotnet/csharplang/discussions/4536"&gt;https://github.com/dotnet/csharplang/discussions/4536&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
      <pubDate>Sun, 14 Mar 2021 04:46:30 Z</pubDate>
    </item>
    <item>
      <guid isPermaLink="true">https://github.com/dotnet/csharplang/issues/4530</guid>
      <link>https://github.com/dotnet/csharplang/issues/4530</link>
      <title>Issue: [Proposal]: Inlined enumeration as function argument expression - @AlFasGD</title>
      <description>&lt;h1&gt;Inlined enumeration as function argument expression&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;[x] Proposed&lt;/li&gt;
&lt;li&gt;[ ] Prototype: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Implementation: Not Started&lt;/li&gt;
&lt;li&gt;[ ] Specification: Not Started&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;p&gt;This syntax should provide a faster way to call a function for each element in an enumerable object.&lt;/p&gt;
&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;The main motive behind this syntax is simplifying the code that is required to properly enumerate through a collection of objects and only call one function within. In other words, this aims to simplify the process of performing a single operation on a collection of objects.&lt;/p&gt;
&lt;h2&gt;Detailed design&lt;/h2&gt;
&lt;p&gt;Assuming that an operation is performed like so:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var x in items)
    Function(x);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;the new syntax simplifies this code into:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Function(foreach in items);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This skips the need for introducing a temporary variable into the mix, as its only purpose is to be used once.&lt;/p&gt;
&lt;hr /&gt;
&lt;p&gt;For functions with multiple arguments, the &lt;code&gt;foreach in&lt;/code&gt; statement should be placed before the collection whose iterated elements are intended to be used as an argument at the desired position. For instance:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var x in items)
    Function(1, x);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Function(1, foreach in items);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;Multiple &lt;code&gt;foreach in&lt;/code&gt; can be allowed per single function call, but it becomes the equivalent of nesting each &lt;code&gt;foreach&lt;/code&gt; loop in each other. Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var x in itemsX)
    foreach (var y in itemsY)
        Function(x, y);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Function(foreach in itemsX, foreach in itemsY);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;Multiple &lt;code&gt;foreach in&lt;/code&gt; can also be present in &lt;code&gt;params&lt;/code&gt; args. Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;// assume the function:
void Register(params Item[] items);

// usage code:
foreach (var x in itemsX)
foreach (var y in itemsY)
foreach (var z in itemsZ)
foreach (var w in itemsW)
    Register(x, y, z, w);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Register(foreach in itemsX, foreach in itemsY, foreach in itemsZ, foreach in itemsW);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;Nested and chained function call expressions are also supported.
An example of a chained function call expression:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var c in chars)
    stringBuilder.Append(c).Append(' ');
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;stringBuilder.Append(foreach in chars).Append(' ');
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;And an example of a nested function call expression:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var loc in locations)
    Register(Invert(loc));
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;Register(Invert(foreach in locations));
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;There is also the ability to apply this for &lt;code&gt;IAsyncEnumerable&lt;/code&gt; objects, by using the &lt;code&gt;await foreach in&lt;/code&gt; variant. Example:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;await foreach (var x in items)
    Function(x);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;becomes:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;Function(await foreach in items);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Drawbacks&lt;/h2&gt;
&lt;p&gt;This syntax does not break compatibility with other rules. It is however a relatively niche feature, and despite it not requiring changes outside Roslyn itself, it could not prove worthy of existing.&lt;/p&gt;
&lt;p&gt;One issue is from supporting multiple &lt;code&gt;foreach in&lt;/code&gt; in a single call, which may increase line size for cases when more than 2 are used, sacficing code readability.&lt;/p&gt;
&lt;h2&gt;Alternatives&lt;/h2&gt;
&lt;p&gt;Another proposed design is only offering a shorthand for:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-csharp"&gt;foreach (var x in items)
    Function(x);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;into:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Function foreach in items;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Same conditionals apply, but this syntax only works for functions that take exactly one argument. It is not clear if nested or chained function call expressions can be supported, or what new features and syntax would have to be introduced for that to work.&lt;/p&gt;
&lt;h2&gt;Unresolved questions&lt;/h2&gt;
&lt;p&gt;None yet.&lt;/p&gt;
&lt;h2&gt;Design meetings&lt;/h2&gt;
&lt;p&gt;None.&lt;/p&gt;
</description>
      <pubDate>Fri, 12 Mar 2021 15:30:11 Z</pubDate>
    </item>
  </channel>
</rss>